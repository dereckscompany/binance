# Generated by roxyassert: do not edit by hand.
# Re-generated on devtools::document(). Commit this file like NAMESPACE.

assert_args_BinanceAccount__get_account_info <- function(recv_window) {
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceAccount__get_account_info <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("maker_commission", "taker_commission", "buyer_commission", "seller_commission", "commission_rates_maker", "commission_rates_taker", "commission_rates_buyer", "commission_rates_seller", "can_trade", "can_withdraw", "can_deposit", "brokered", "require_self_trade_prevention", "prevent_sor", "update_time", "account_type", "permissions", "uid"))
  assert_integer(value[["maker_commission"]])
  assert_no_missing_values(value[["maker_commission"]])
  assert_integer(value[["taker_commission"]])
  assert_no_missing_values(value[["taker_commission"]])
  assert_integer(value[["buyer_commission"]])
  assert_no_missing_values(value[["buyer_commission"]])
  assert_integer(value[["seller_commission"]])
  assert_no_missing_values(value[["seller_commission"]])
  assert_character(value[["commission_rates_maker"]])
  assert_no_missing_values(value[["commission_rates_maker"]])
  assert_character(value[["commission_rates_taker"]])
  assert_no_missing_values(value[["commission_rates_taker"]])
  assert_character(value[["commission_rates_buyer"]])
  assert_no_missing_values(value[["commission_rates_buyer"]])
  assert_character(value[["commission_rates_seller"]])
  assert_no_missing_values(value[["commission_rates_seller"]])
  assert_logical(value[["can_trade"]])
  assert_no_missing_values(value[["can_trade"]])
  assert_logical(value[["can_withdraw"]])
  assert_no_missing_values(value[["can_withdraw"]])
  assert_logical(value[["can_deposit"]])
  assert_no_missing_values(value[["can_deposit"]])
  assert_logical(value[["brokered"]])
  assert_no_missing_values(value[["brokered"]])
  assert_logical(value[["require_self_trade_prevention"]])
  assert_no_missing_values(value[["require_self_trade_prevention"]])
  assert_logical(value[["prevent_sor"]])
  assert_no_missing_values(value[["prevent_sor"]])
  assert_datetime(value[["update_time"]])
  assert_no_missing_values(value[["update_time"]])
  assert_character(value[["account_type"]])
  assert_no_missing_values(value[["account_type"]])
  assert_character(value[["permissions"]])
  assert_no_missing_values(value[["permissions"]])
  assert_double(value[["uid"]])
  assert_no_missing_values(value[["uid"]])
  return(value)
}

assert_args_BinanceAccount__get_balances <- function(omit_zero_balances, recv_window) {
  if (!is.null(omit_zero_balances)) {
    assert_scalar_logical(omit_zero_balances)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceAccount__get_balances <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("asset", "free", "locked"))
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_character(value[["free"]])
  assert_no_missing_values(value[["free"]])
  assert_character(value[["locked"]])
  assert_no_missing_values(value[["locked"]])
  return(value)
}

assert_args_BinanceAccount__get_trades <- function(symbol, order_id, start_time, end_time, from_id, limit, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(order_id)) {
    assert_scalar_count(order_id)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(from_id)) {
    assert_scalar_count(from_id)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceAccount__get_trades <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "id", "order_id", "order_list_id", "price", "qty", "quote_qty", "commission", "commission_asset", "is_buyer", "is_maker", "is_best_match", "time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_double(value[["order_list_id"]])
  assert_no_missing_values(value[["order_list_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["qty"]])
  assert_no_missing_values(value[["qty"]])
  assert_character(value[["quote_qty"]])
  assert_no_missing_values(value[["quote_qty"]])
  assert_character(value[["commission"]])
  assert_no_missing_values(value[["commission"]])
  assert_character(value[["commission_asset"]])
  assert_no_missing_values(value[["commission_asset"]])
  assert_logical(value[["is_buyer"]])
  assert_no_missing_values(value[["is_buyer"]])
  assert_logical(value[["is_maker"]])
  assert_no_missing_values(value[["is_maker"]])
  assert_logical(value[["is_best_match"]])
  assert_no_missing_values(value[["is_best_match"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  return(value)
}

assert_args_BinanceBase__initialize <- function(keys, base_url, async, time_source) {
  assert_list(keys)
  assert_scalar_character(base_url)
  assert_scalar_logical(async)
  assert_scalar_character(time_source)
  assert_value_in_set(time_source, c("local", "server"))
  return(invisible(NULL))
}

assert_return_BinanceBase__initialize <- function(value) {
  assert_class(value, "BinanceBase")
  return(value)
}

assert_args_BinanceDeposit__get_deposit_address <- function(coin, network, recv_window) {
  assert_scalar_character(coin)
  if (!is.null(network)) {
    assert_scalar_character(network)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
    assert_between(recv_window, lower = 1)
  }
  return(invisible(NULL))
}

assert_return_BinanceDeposit__get_deposit_address <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("address", "coin", "tag", "url"))
  assert_character(value[["address"]])
  assert_no_missing_values(value[["address"]])
  assert_character(value[["coin"]])
  assert_no_missing_values(value[["coin"]])
  assert_character(value[["tag"]])
  assert_no_missing_values(value[["tag"]])
  assert_character(value[["url"]])
  assert_no_missing_values(value[["url"]])
  return(value)
}

assert_args_BinanceDeposit__get_deposit_history <- function(coin, status, start_time, end_time, offset, limit, tx_id, recv_window) {
  if (!is.null(coin)) {
    assert_scalar_character(coin)
  }
  if (!is.null(status)) {
    assert_scalar_count(status)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(offset)) {
    assert_scalar_count(offset)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(tx_id)) {
    assert_scalar_character(tx_id)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceDeposit__get_deposit_history <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id", "amount", "coin", "network", "status", "address", "address_tag", "tx_id", "transfer_type", "confirm_times", "unlock_confirm", "wallet_type", "insert_time", "complete_time"))
  assert_character(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_character(value[["amount"]])
  assert_no_missing_values(value[["amount"]])
  assert_character(value[["coin"]])
  assert_no_missing_values(value[["coin"]])
  assert_character(value[["network"]])
  assert_no_missing_values(value[["network"]])
  assert_integer(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["address"]])
  assert_no_missing_values(value[["address"]])
  assert_character(value[["address_tag"]])
  assert_no_missing_values(value[["address_tag"]])
  assert_character(value[["tx_id"]])
  assert_no_missing_values(value[["tx_id"]])
  assert_integer(value[["transfer_type"]])
  assert_no_missing_values(value[["transfer_type"]])
  assert_character(value[["confirm_times"]])
  assert_no_missing_values(value[["confirm_times"]])
  assert_integer(value[["unlock_confirm"]])
  assert_no_missing_values(value[["unlock_confirm"]])
  assert_integer(value[["wallet_type"]])
  assert_no_missing_values(value[["wallet_type"]])
  assert_datetime(value[["insert_time"]])
  assert_no_missing_values(value[["insert_time"]])
  assert_datetime(value[["complete_time"]])
  assert_no_missing_values(value[["complete_time"]])
  return(value)
}

assert_args_BinanceEarn__get_flexible_products <- function(asset, current, size, recv_window) {
  if (!is.null(asset)) {
    assert_scalar_character(asset)
  }
  if (!is.null(current)) {
    assert_scalar_count(current)
  }
  if (!is.null(size)) {
    assert_scalar_count(size)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceEarn__get_flexible_products <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("asset", "latest_annual_percentage_rate", "tier_annual_percentage_rate", "can_purchase", "can_redeem", "is_sold_out", "hot", "min_purchase_amount", "product_id", "subscription_start_time", "status"))
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_character(value[["latest_annual_percentage_rate"]])
  assert_no_missing_values(value[["latest_annual_percentage_rate"]])
  assert_character(value[["tier_annual_percentage_rate"]])
  assert_logical(value[["can_purchase"]])
  assert_no_missing_values(value[["can_purchase"]])
  assert_logical(value[["can_redeem"]])
  assert_no_missing_values(value[["can_redeem"]])
  assert_logical(value[["is_sold_out"]])
  assert_no_missing_values(value[["is_sold_out"]])
  assert_logical(value[["hot"]])
  assert_no_missing_values(value[["hot"]])
  assert_character(value[["min_purchase_amount"]])
  assert_no_missing_values(value[["min_purchase_amount"]])
  assert_character(value[["product_id"]])
  assert_no_missing_values(value[["product_id"]])
  assert_datetime(value[["subscription_start_time"]])
  assert_no_missing_values(value[["subscription_start_time"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  return(value)
}

assert_args_BinanceEarn__get_locked_products <- function(asset, current, size, recv_window) {
  if (!is.null(asset)) {
    assert_scalar_character(asset)
  }
  if (!is.null(current)) {
    assert_scalar_count(current)
  }
  if (!is.null(size)) {
    assert_scalar_count(size)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceEarn__get_locked_products <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("project_id", "detail_asset", "detail_reward_asset", "detail_duration", "detail_renewable", "detail_is_sold_out", "detail_apr", "detail_status", "detail_subscription_start_time", "detail_extra_reward_asset", "detail_extra_reward_apr", "detail_boost_reward_asset", "detail_boost_apr", "detail_boost_end_time", "quota_total_personal_quota", "quota_minimum"))
  assert_character(value[["project_id"]])
  assert_no_missing_values(value[["project_id"]])
  assert_character(value[["detail_asset"]])
  assert_no_missing_values(value[["detail_asset"]])
  assert_character(value[["detail_reward_asset"]])
  assert_no_missing_values(value[["detail_reward_asset"]])
  assert_integer(value[["detail_duration"]])
  assert_no_missing_values(value[["detail_duration"]])
  assert_logical(value[["detail_renewable"]])
  assert_no_missing_values(value[["detail_renewable"]])
  assert_logical(value[["detail_is_sold_out"]])
  assert_no_missing_values(value[["detail_is_sold_out"]])
  assert_character(value[["detail_apr"]])
  assert_no_missing_values(value[["detail_apr"]])
  assert_character(value[["detail_status"]])
  assert_no_missing_values(value[["detail_status"]])
  assert_datetime(value[["detail_subscription_start_time"]])
  assert_no_missing_values(value[["detail_subscription_start_time"]])
  assert_character(value[["detail_extra_reward_asset"]])
  assert_no_missing_values(value[["detail_extra_reward_asset"]])
  assert_character(value[["detail_extra_reward_apr"]])
  assert_no_missing_values(value[["detail_extra_reward_apr"]])
  assert_character(value[["detail_boost_reward_asset"]])
  assert_no_missing_values(value[["detail_boost_reward_asset"]])
  assert_character(value[["detail_boost_apr"]])
  assert_no_missing_values(value[["detail_boost_apr"]])
  assert_datetime(value[["detail_boost_end_time"]])
  assert_no_missing_values(value[["detail_boost_end_time"]])
  assert_character(value[["quota_total_personal_quota"]])
  assert_no_missing_values(value[["quota_total_personal_quota"]])
  assert_character(value[["quota_minimum"]])
  assert_no_missing_values(value[["quota_minimum"]])
  return(value)
}

assert_args_BinanceEarn__add_flexible_subscription <- function(product_id, amount, auto_subscribe, source_account, recv_window) {
  assert_scalar_character(product_id)
  assert_scalar_double(amount)
  if (!is.null(auto_subscribe)) {
    assert_scalar_logical(auto_subscribe)
  }
  if (!is.null(source_account)) {
    assert_scalar_character(source_account)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceEarn__add_flexible_subscription <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("purchase_id", "success"))
  assert_double(value[["purchase_id"]])
  assert_no_missing_values(value[["purchase_id"]])
  assert_logical(value[["success"]])
  assert_no_missing_values(value[["success"]])
  return(value)
}

assert_args_BinanceEarn__add_locked_subscription <- function(project_id, amount, auto_subscribe, recv_window) {
  assert_scalar_character(project_id)
  assert_scalar_double(amount)
  if (!is.null(auto_subscribe)) {
    assert_scalar_logical(auto_subscribe)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceEarn__add_locked_subscription <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("purchase_id", "position_id", "success"))
  assert_double(value[["purchase_id"]])
  assert_no_missing_values(value[["purchase_id"]])
  assert_character(value[["position_id"]])
  assert_no_missing_values(value[["position_id"]])
  assert_logical(value[["success"]])
  assert_no_missing_values(value[["success"]])
  return(value)
}

assert_args_BinanceEarn__add_flexible_redemption <- function(product_id, amount, redeem_all, dest_account, recv_window) {
  assert_scalar_character(product_id)
  if (!is.null(amount)) {
    assert_scalar_double(amount)
  }
  if (!is.null(redeem_all)) {
    assert_scalar_logical(redeem_all)
  }
  if (!is.null(dest_account)) {
    assert_scalar_character(dest_account)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceEarn__add_flexible_redemption <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("redeem_id", "success"))
  assert_double(value[["redeem_id"]])
  assert_no_missing_values(value[["redeem_id"]])
  assert_logical(value[["success"]])
  assert_no_missing_values(value[["success"]])
  return(value)
}

assert_args_BinanceEarn__add_locked_redemption <- function(position_id, recv_window) {
  assert_scalar_character(position_id)
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceEarn__add_locked_redemption <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("redeem_id", "success"))
  assert_double(value[["redeem_id"]])
  assert_no_missing_values(value[["redeem_id"]])
  assert_logical(value[["success"]])
  assert_no_missing_values(value[["success"]])
  return(value)
}

assert_args_BinanceEarn__get_flexible_position <- function(asset, product_id, current, size, recv_window) {
  if (!is.null(asset)) {
    assert_scalar_character(asset)
  }
  if (!is.null(product_id)) {
    assert_scalar_character(product_id)
  }
  if (!is.null(current)) {
    assert_scalar_count(current)
  }
  if (!is.null(size)) {
    assert_scalar_count(size)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceEarn__get_flexible_position <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("total_amount", "latest_annual_percentage_rate", "tier_annual_percentage_rate", "yesterday_airdrop_percentage_rate", "asset", "air_drop_asset", "can_redeem", "collateral_amount", "product_id", "yesterday_real_time_rewards", "cumulative_bonus_rewards", "cumulative_real_time_rewards", "cumulative_total_rewards", "auto_subscribe"))
  assert_character(value[["total_amount"]])
  assert_no_missing_values(value[["total_amount"]])
  assert_character(value[["latest_annual_percentage_rate"]])
  assert_no_missing_values(value[["latest_annual_percentage_rate"]])
  assert_character(value[["tier_annual_percentage_rate"]])
  assert_character(value[["yesterday_airdrop_percentage_rate"]])
  assert_no_missing_values(value[["yesterday_airdrop_percentage_rate"]])
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_character(value[["air_drop_asset"]])
  assert_no_missing_values(value[["air_drop_asset"]])
  assert_logical(value[["can_redeem"]])
  assert_no_missing_values(value[["can_redeem"]])
  assert_character(value[["collateral_amount"]])
  assert_no_missing_values(value[["collateral_amount"]])
  assert_character(value[["product_id"]])
  assert_no_missing_values(value[["product_id"]])
  assert_character(value[["yesterday_real_time_rewards"]])
  assert_no_missing_values(value[["yesterday_real_time_rewards"]])
  assert_character(value[["cumulative_bonus_rewards"]])
  assert_no_missing_values(value[["cumulative_bonus_rewards"]])
  assert_character(value[["cumulative_real_time_rewards"]])
  assert_no_missing_values(value[["cumulative_real_time_rewards"]])
  assert_character(value[["cumulative_total_rewards"]])
  assert_no_missing_values(value[["cumulative_total_rewards"]])
  assert_logical(value[["auto_subscribe"]])
  assert_no_missing_values(value[["auto_subscribe"]])
  return(value)
}

assert_args_BinanceEarn__get_locked_position <- function(asset, position_id, project_id, current, size, recv_window) {
  if (!is.null(asset)) {
    assert_scalar_character(asset)
  }
  if (!is.null(position_id)) {
    assert_scalar_character(position_id)
  }
  if (!is.null(project_id)) {
    assert_scalar_character(project_id)
  }
  if (!is.null(current)) {
    assert_scalar_count(current)
  }
  if (!is.null(size)) {
    assert_scalar_count(size)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceEarn__get_locked_position <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_BinanceEarn__get_flexible_subscription_history <- function(product_id, purchase_id, asset, start_time, end_time, current, size, recv_window) {
  if (!is.null(product_id)) {
    assert_scalar_character(product_id)
  }
  if (!is.null(purchase_id)) {
    assert_scalar_count(purchase_id)
  }
  if (!is.null(asset)) {
    assert_scalar_character(asset)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(current)) {
    assert_scalar_count(current)
  }
  if (!is.null(size)) {
    assert_scalar_count(size)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceEarn__get_flexible_subscription_history <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("amount", "asset", "time", "purchase_id", "type", "source_account", "status"))
  assert_character(value[["amount"]])
  assert_no_missing_values(value[["amount"]])
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_double(value[["purchase_id"]])
  assert_no_missing_values(value[["purchase_id"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["source_account"]])
  assert_no_missing_values(value[["source_account"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  return(value)
}

assert_args_BinanceEarn__get_locked_subscription_history <- function(purchase_id, asset, start_time, end_time, current, size, recv_window) {
  if (!is.null(purchase_id)) {
    assert_scalar_count(purchase_id)
  }
  if (!is.null(asset)) {
    assert_scalar_character(asset)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(current)) {
    assert_scalar_count(current)
  }
  if (!is.null(size)) {
    assert_scalar_count(size)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceEarn__get_locked_subscription_history <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("amount", "asset", "time", "purchase_id", "position_id", "lock_period", "type", "source_account", "status"))
  assert_character(value[["amount"]])
  assert_no_missing_values(value[["amount"]])
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_double(value[["purchase_id"]])
  assert_no_missing_values(value[["purchase_id"]])
  assert_character(value[["position_id"]])
  assert_no_missing_values(value[["position_id"]])
  assert_integer(value[["lock_period"]])
  assert_no_missing_values(value[["lock_period"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["source_account"]])
  assert_no_missing_values(value[["source_account"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  return(value)
}

assert_args_BinanceEarn__get_flexible_redemption_history <- function(product_id, redeem_id, asset, start_time, end_time, current, size, recv_window) {
  if (!is.null(product_id)) {
    assert_scalar_character(product_id)
  }
  if (!is.null(redeem_id)) {
    assert_scalar_count(redeem_id)
  }
  if (!is.null(asset)) {
    assert_scalar_character(asset)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(current)) {
    assert_scalar_count(current)
  }
  if (!is.null(size)) {
    assert_scalar_count(size)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceEarn__get_flexible_redemption_history <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("amount", "asset", "time", "project_id", "redeem_id", "dest_account", "status"))
  assert_character(value[["amount"]])
  assert_no_missing_values(value[["amount"]])
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_character(value[["project_id"]])
  assert_no_missing_values(value[["project_id"]])
  assert_double(value[["redeem_id"]])
  assert_no_missing_values(value[["redeem_id"]])
  assert_character(value[["dest_account"]])
  assert_no_missing_values(value[["dest_account"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  return(value)
}

assert_args_BinanceEarn__get_locked_redemption_history <- function(position_id, redeem_id, asset, start_time, end_time, current, size, recv_window) {
  if (!is.null(position_id)) {
    assert_scalar_character(position_id)
  }
  if (!is.null(redeem_id)) {
    assert_scalar_count(redeem_id)
  }
  if (!is.null(asset)) {
    assert_scalar_character(asset)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(current)) {
    assert_scalar_count(current)
  }
  if (!is.null(size)) {
    assert_scalar_count(size)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceEarn__get_locked_redemption_history <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("amount", "asset", "time", "position_id", "redeem_id", "deliver_date", "status"))
  assert_character(value[["amount"]])
  assert_no_missing_values(value[["amount"]])
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_character(value[["position_id"]])
  assert_no_missing_values(value[["position_id"]])
  assert_double(value[["redeem_id"]])
  assert_no_missing_values(value[["redeem_id"]])
  assert_datetime(value[["deliver_date"]])
  assert_no_missing_values(value[["deliver_date"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  return(value)
}

assert_args_BinanceFutures__initialize <- function(keys, base_url, async, time_source) {
  assert_list(keys)
  assert_scalar_character(base_url)
  assert_scalar_logical(async)
  assert_scalar_character(time_source)
  return(invisible(NULL))
}

assert_return_BinanceFutures__initialize <- function(value) {
  assert_class(value, "BinanceFutures")
  return(value)
}

assert_args_BinanceFutures__add_order <- function(symbol, side, type, quantity, price, stop_price, time_in_force, position_side, reduce_only, new_client_order_id, close_position, working_type, new_order_resp_type, recv_window) {
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  assert_scalar_character(type)
  if (!is.null(quantity)) {
    assert_scalar_double(quantity)
  }
  if (!is.null(price)) {
    assert_scalar_double(price)
  }
  if (!is.null(stop_price)) {
    assert_scalar_double(stop_price)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(position_side)) {
    assert_scalar_character(position_side)
  }
  if (!is.null(reduce_only)) {
    assert_scalar_logical(reduce_only)
  }
  if (!is.null(new_client_order_id)) {
    assert_scalar_character(new_client_order_id)
  }
  if (!is.null(close_position)) {
    assert_scalar_logical(close_position)
  }
  if (!is.null(working_type)) {
    assert_scalar_character(working_type)
  }
  if (!is.null(new_order_resp_type)) {
    assert_scalar_character(new_order_resp_type)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__add_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "client_order_id", "price", "orig_qty", "executed_qty", "cum_quote", "status", "time_in_force", "type", "side", "position_side", "update_time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["cum_quote"]])
  assert_no_missing_values(value[["cum_quote"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["time_in_force"]])
  assert_no_missing_values(value[["time_in_force"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_character(value[["position_side"]])
  assert_no_missing_values(value[["position_side"]])
  assert_datetime(value[["update_time"]])
  assert_no_missing_values(value[["update_time"]])
  return(value)
}

assert_args_BinanceFutures__add_order_test <- function(symbol, side, type, quantity, price, stop_price, time_in_force, position_side, reduce_only, new_client_order_id, close_position, working_type, new_order_resp_type, recv_window) {
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  assert_scalar_character(type)
  if (!is.null(quantity)) {
    assert_scalar_double(quantity)
  }
  if (!is.null(price)) {
    assert_scalar_double(price)
  }
  if (!is.null(stop_price)) {
    assert_scalar_double(stop_price)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(position_side)) {
    assert_scalar_character(position_side)
  }
  if (!is.null(reduce_only)) {
    assert_scalar_logical(reduce_only)
  }
  if (!is.null(new_client_order_id)) {
    assert_scalar_character(new_client_order_id)
  }
  if (!is.null(close_position)) {
    assert_scalar_logical(close_position)
  }
  if (!is.null(working_type)) {
    assert_scalar_character(working_type)
  }
  if (!is.null(new_order_resp_type)) {
    assert_scalar_character(new_order_resp_type)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__add_order_test <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("validated"))
  assert_logical(value[["validated"]])
  assert_no_missing_values(value[["validated"]])
  return(value)
}

assert_args_BinanceFutures__cancel_order <- function(symbol, order_id, orig_client_order_id, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(order_id)) {
    assert_scalar_count(order_id)
  }
  if (!is.null(orig_client_order_id)) {
    assert_scalar_character(orig_client_order_id)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__cancel_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "client_order_id", "orig_qty", "executed_qty", "status", "type", "side", "update_time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_datetime(value[["update_time"]])
  assert_no_missing_values(value[["update_time"]])
  return(value)
}

assert_args_BinanceFutures__cancel_all_orders <- function(symbol, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__cancel_all_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("code", "msg"))
  assert_integer(value[["code"]])
  assert_no_missing_values(value[["code"]])
  assert_character(value[["msg"]])
  assert_no_missing_values(value[["msg"]])
  return(value)
}

assert_args_BinanceFutures__get_order <- function(symbol, order_id, orig_client_order_id, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(order_id)) {
    assert_scalar_count(order_id)
  }
  if (!is.null(orig_client_order_id)) {
    assert_scalar_character(orig_client_order_id)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__get_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "client_order_id", "price", "orig_qty", "executed_qty", "cum_quote", "status", "time_in_force", "type", "side", "position_side", "time", "update_time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["cum_quote"]])
  assert_no_missing_values(value[["cum_quote"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["time_in_force"]])
  assert_no_missing_values(value[["time_in_force"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_character(value[["position_side"]])
  assert_no_missing_values(value[["position_side"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_datetime(value[["update_time"]])
  assert_no_missing_values(value[["update_time"]])
  return(value)
}

assert_args_BinanceFutures__get_open_orders <- function(symbol, recv_window) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__get_open_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "client_order_id", "price", "orig_qty", "executed_qty", "status", "type", "side", "position_side", "time", "update_time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_character(value[["position_side"]])
  assert_no_missing_values(value[["position_side"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_datetime(value[["update_time"]])
  assert_no_missing_values(value[["update_time"]])
  return(value)
}

assert_args_BinanceFutures__get_all_orders <- function(symbol, order_id, start_time, end_time, limit, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(order_id)) {
    assert_scalar_count(order_id)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__get_all_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "client_order_id", "price", "orig_qty", "executed_qty", "status", "type", "side", "position_side", "time", "update_time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_character(value[["position_side"]])
  assert_no_missing_values(value[["position_side"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_datetime(value[["update_time"]])
  assert_no_missing_values(value[["update_time"]])
  return(value)
}

assert_args_BinanceFutures__get_account <- function(recv_window) {
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__get_account <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("fee_tier", "can_trade", "can_deposit", "can_withdraw", "total_initial_margin", "total_maint_margin", "total_wallet_balance", "total_unrealized_profit", "total_margin_balance", "total_cross_wallet_balance", "available_balance", "max_withdraw_amount", "asset_asset", "asset_wallet_balance", "asset_unrealized_profit", "asset_margin_balance", "asset_available_balance"))
  assert_integer(value[["fee_tier"]])
  assert_no_missing_values(value[["fee_tier"]])
  assert_logical(value[["can_trade"]])
  assert_no_missing_values(value[["can_trade"]])
  assert_logical(value[["can_deposit"]])
  assert_no_missing_values(value[["can_deposit"]])
  assert_logical(value[["can_withdraw"]])
  assert_no_missing_values(value[["can_withdraw"]])
  assert_character(value[["total_initial_margin"]])
  assert_no_missing_values(value[["total_initial_margin"]])
  assert_character(value[["total_maint_margin"]])
  assert_no_missing_values(value[["total_maint_margin"]])
  assert_character(value[["total_wallet_balance"]])
  assert_no_missing_values(value[["total_wallet_balance"]])
  assert_character(value[["total_unrealized_profit"]])
  assert_no_missing_values(value[["total_unrealized_profit"]])
  assert_character(value[["total_margin_balance"]])
  assert_no_missing_values(value[["total_margin_balance"]])
  assert_character(value[["total_cross_wallet_balance"]])
  assert_no_missing_values(value[["total_cross_wallet_balance"]])
  assert_character(value[["available_balance"]])
  assert_no_missing_values(value[["available_balance"]])
  assert_character(value[["max_withdraw_amount"]])
  assert_no_missing_values(value[["max_withdraw_amount"]])
  assert_character(value[["asset_asset"]])
  assert_no_missing_values(value[["asset_asset"]])
  assert_character(value[["asset_wallet_balance"]])
  assert_no_missing_values(value[["asset_wallet_balance"]])
  assert_character(value[["asset_unrealized_profit"]])
  assert_no_missing_values(value[["asset_unrealized_profit"]])
  assert_character(value[["asset_margin_balance"]])
  assert_no_missing_values(value[["asset_margin_balance"]])
  assert_character(value[["asset_available_balance"]])
  assert_no_missing_values(value[["asset_available_balance"]])
  return(value)
}

assert_args_BinanceFutures__get_balances <- function(recv_window) {
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__get_balances <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("account_alias", "asset", "balance", "cross_wallet_balance", "cross_un_pnl", "available_balance", "max_withdraw_amount", "margin_available", "update_time"))
  assert_character(value[["account_alias"]])
  assert_no_missing_values(value[["account_alias"]])
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_character(value[["balance"]])
  assert_no_missing_values(value[["balance"]])
  assert_character(value[["cross_wallet_balance"]])
  assert_no_missing_values(value[["cross_wallet_balance"]])
  assert_character(value[["cross_un_pnl"]])
  assert_no_missing_values(value[["cross_un_pnl"]])
  assert_character(value[["available_balance"]])
  assert_no_missing_values(value[["available_balance"]])
  assert_character(value[["max_withdraw_amount"]])
  assert_no_missing_values(value[["max_withdraw_amount"]])
  assert_logical(value[["margin_available"]])
  assert_no_missing_values(value[["margin_available"]])
  assert_datetime(value[["update_time"]])
  assert_no_missing_values(value[["update_time"]])
  return(value)
}

assert_args_BinanceFutures__get_positions <- function(symbol, recv_window) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__get_positions <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "position_side", "position_amt", "entry_price", "mark_price", "un_realized_profit", "liquidation_price", "leverage", "margin_type", "isolated_margin", "notional", "update_time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["position_side"]])
  assert_no_missing_values(value[["position_side"]])
  assert_character(value[["position_amt"]])
  assert_no_missing_values(value[["position_amt"]])
  assert_character(value[["entry_price"]])
  assert_no_missing_values(value[["entry_price"]])
  assert_character(value[["mark_price"]])
  assert_no_missing_values(value[["mark_price"]])
  assert_character(value[["un_realized_profit"]])
  assert_no_missing_values(value[["un_realized_profit"]])
  assert_character(value[["liquidation_price"]])
  assert_no_missing_values(value[["liquidation_price"]])
  assert_character(value[["leverage"]])
  assert_no_missing_values(value[["leverage"]])
  assert_character(value[["margin_type"]])
  assert_no_missing_values(value[["margin_type"]])
  assert_character(value[["isolated_margin"]])
  assert_no_missing_values(value[["isolated_margin"]])
  assert_character(value[["notional"]])
  assert_no_missing_values(value[["notional"]])
  assert_datetime(value[["update_time"]])
  assert_no_missing_values(value[["update_time"]])
  return(value)
}

assert_args_BinanceFutures__set_leverage <- function(symbol, leverage, recv_window) {
  assert_scalar_character(symbol)
  assert_scalar_count(leverage)
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__set_leverage <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("leverage", "max_notional_value", "symbol"))
  assert_integer(value[["leverage"]])
  assert_no_missing_values(value[["leverage"]])
  assert_character(value[["max_notional_value"]])
  assert_no_missing_values(value[["max_notional_value"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  return(value)
}

assert_args_BinanceFutures__set_margin_type <- function(symbol, margin_type, recv_window) {
  assert_scalar_character(symbol)
  assert_scalar_character(margin_type)
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__set_margin_type <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("code", "msg"))
  assert_integer(value[["code"]])
  assert_no_missing_values(value[["code"]])
  assert_character(value[["msg"]])
  assert_no_missing_values(value[["msg"]])
  return(value)
}

assert_args_BinanceFutures__modify_position_margin <- function(symbol, amount, type, position_side, recv_window) {
  assert_scalar_character(symbol)
  assert_scalar_double(amount)
  assert_scalar_count(type)
  if (!is.null(position_side)) {
    assert_scalar_character(position_side)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__modify_position_margin <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("code", "msg", "amount", "type"))
  assert_integer(value[["code"]])
  assert_no_missing_values(value[["code"]])
  assert_character(value[["msg"]])
  assert_no_missing_values(value[["msg"]])
  assert_double(value[["amount"]])
  assert_no_missing_values(value[["amount"]])
  assert_integer(value[["type"]])
  assert_no_missing_values(value[["type"]])
  return(value)
}

assert_args_BinanceFutures__get_position_margin_history <- function(symbol, type, start_time, end_time, limit, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(type)) {
    assert_scalar_count(type)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__get_position_margin_history <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "type", "delta_type", "amount", "asset", "time", "position_side"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_integer(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["delta_type"]])
  assert_no_missing_values(value[["delta_type"]])
  assert_character(value[["amount"]])
  assert_no_missing_values(value[["amount"]])
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_character(value[["position_side"]])
  assert_no_missing_values(value[["position_side"]])
  return(value)
}

assert_args_BinanceFutures__get_trades <- function(symbol, order_id, start_time, end_time, from_id, limit, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(order_id)) {
    assert_scalar_count(order_id)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(from_id)) {
    assert_scalar_count(from_id)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__get_trades <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "id", "order_id", "price", "qty", "quote_qty", "commission", "commission_asset", "realized_pnl", "side", "position_side", "buyer", "maker", "time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["qty"]])
  assert_no_missing_values(value[["qty"]])
  assert_character(value[["quote_qty"]])
  assert_no_missing_values(value[["quote_qty"]])
  assert_character(value[["commission"]])
  assert_no_missing_values(value[["commission"]])
  assert_character(value[["commission_asset"]])
  assert_no_missing_values(value[["commission_asset"]])
  assert_character(value[["realized_pnl"]])
  assert_no_missing_values(value[["realized_pnl"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_character(value[["position_side"]])
  assert_no_missing_values(value[["position_side"]])
  assert_logical(value[["buyer"]])
  assert_no_missing_values(value[["buyer"]])
  assert_logical(value[["maker"]])
  assert_no_missing_values(value[["maker"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  return(value)
}

assert_args_BinanceFutures__get_income_history <- function(symbol, income_type, start_time, end_time, limit, recv_window) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  if (!is.null(income_type)) {
    assert_scalar_character(income_type)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__get_income_history <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "income_type", "income", "asset", "info", "time", "tran_id", "trade_id"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["income_type"]])
  assert_no_missing_values(value[["income_type"]])
  assert_character(value[["income"]])
  assert_no_missing_values(value[["income"]])
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_character(value[["info"]])
  assert_no_missing_values(value[["info"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_double(value[["tran_id"]])
  assert_no_missing_values(value[["tran_id"]])
  assert_character(value[["trade_id"]])
  assert_no_missing_values(value[["trade_id"]])
  return(value)
}

assert_args_BinanceFutures__set_position_mode <- function(dual_side_position, recv_window) {
  assert_scalar_logical(dual_side_position)
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__set_position_mode <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("code", "msg"))
  assert_integer(value[["code"]])
  assert_no_missing_values(value[["code"]])
  assert_character(value[["msg"]])
  assert_no_missing_values(value[["msg"]])
  return(value)
}

assert_args_BinanceFutures__get_position_mode <- function(recv_window) {
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceFutures__get_position_mode <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("dual_side_position"))
  assert_logical(value[["dual_side_position"]])
  assert_no_missing_values(value[["dual_side_position"]])
  return(value)
}

assert_args_BinanceFuturesData__initialize <- function(keys, base_url, async, time_source) {
  assert_list(keys)
  assert_scalar_character(base_url)
  assert_scalar_logical(async)
  assert_scalar_character(time_source)
  return(invisible(NULL))
}

assert_return_BinanceFuturesData__initialize <- function(value) {
  assert_class(value, "BinanceFuturesData")
  return(value)
}

assert_return_BinanceFuturesData__get_exchange_info <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "pair", "contract_type", "status", "base_asset", "quote_asset", "margin_asset", "price_precision", "quantity_precision", "order_types", "time_in_force", "underlying_sub_type", "permission_sets", "lot_min_qty", "lot_max_qty", "lot_step_size", "price_min", "price_max", "price_tick_size", "min_notional", "filters_raw"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["pair"]])
  assert_no_missing_values(value[["pair"]])
  assert_character(value[["contract_type"]])
  assert_no_missing_values(value[["contract_type"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["base_asset"]])
  assert_no_missing_values(value[["base_asset"]])
  assert_character(value[["quote_asset"]])
  assert_no_missing_values(value[["quote_asset"]])
  assert_character(value[["margin_asset"]])
  assert_no_missing_values(value[["margin_asset"]])
  assert_integer(value[["price_precision"]])
  assert_no_missing_values(value[["price_precision"]])
  assert_integer(value[["quantity_precision"]])
  assert_no_missing_values(value[["quantity_precision"]])
  assert_character(value[["order_types"]])
  assert_no_missing_values(value[["order_types"]])
  assert_character(value[["time_in_force"]])
  assert_no_missing_values(value[["time_in_force"]])
  assert_character(value[["underlying_sub_type"]])
  assert_character(value[["permission_sets"]])
  assert_double(value[["lot_min_qty"]])
  assert_double(value[["lot_max_qty"]])
  assert_double(value[["lot_step_size"]])
  assert_double(value[["price_min"]])
  assert_no_missing_values(value[["price_min"]])
  assert_double(value[["price_max"]])
  assert_no_missing_values(value[["price_max"]])
  assert_double(value[["price_tick_size"]])
  assert_no_missing_values(value[["price_tick_size"]])
  assert_double(value[["min_notional"]])
  assert_character(value[["filters_raw"]])
  assert_no_missing_values(value[["filters_raw"]])
  return(value)
}

assert_return_BinanceFuturesData__get_rate_limits <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("rate_limit_type", "interval", "interval_num", "limit"))
  assert_character(value[["rate_limit_type"]])
  assert_no_missing_values(value[["rate_limit_type"]])
  assert_character(value[["interval"]])
  assert_no_missing_values(value[["interval"]])
  assert_integer(value[["interval_num"]])
  assert_no_missing_values(value[["interval_num"]])
  assert_integer(value[["limit"]])
  assert_no_missing_values(value[["limit"]])
  return(value)
}

assert_return_BinanceFuturesData__get_exchange_filters <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_return_BinanceFuturesData__get_futures_assets <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("asset", "margin_available", "auto_asset_exchange"))
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_logical(value[["margin_available"]])
  assert_no_missing_values(value[["margin_available"]])
  assert_character(value[["auto_asset_exchange"]])
  assert_no_missing_values(value[["auto_asset_exchange"]])
  return(value)
}

assert_args_BinanceFuturesData__get_klines <- function(symbol, interval, start_time, end_time, limit, fetch_all, sleep, on_page) {
  assert_scalar_character(symbol)
  assert_scalar_character(interval)
  if (!is.null(start_time)) {
    assert_any_of(
      start_time,
      function(.x) {
        assert_scalar_datetime(.x)
      },
      function(.x) {
        assert_scalar_double(.x)
      }
    )
  }
  if (!is.null(end_time)) {
    assert_any_of(
      end_time,
      function(.x) {
        assert_scalar_datetime(.x)
      },
      function(.x) {
        assert_scalar_double(.x)
      }
    )
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  assert_scalar_logical(fetch_all)
  assert_scalar_double(sleep)
  if (!is.null(on_page)) {
    assert_function(on_page)
  }
  return(invisible(NULL))
}

assert_return_BinanceFuturesData__get_klines <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("datetime", "open", "high", "low", "close", "volume", "close_time", "quote_volume", "trades", "taker_buy_base_volume", "taker_buy_quote_volume", "ignore"))
  assert_datetime(value[["datetime"]])
  assert_no_missing_values(value[["datetime"]])
  assert_double(value[["open"]])
  assert_no_missing_values(value[["open"]])
  assert_double(value[["high"]])
  assert_no_missing_values(value[["high"]])
  assert_double(value[["low"]])
  assert_no_missing_values(value[["low"]])
  assert_double(value[["close"]])
  assert_no_missing_values(value[["close"]])
  assert_double(value[["volume"]])
  assert_no_missing_values(value[["volume"]])
  assert_datetime(value[["close_time"]])
  assert_no_missing_values(value[["close_time"]])
  assert_double(value[["quote_volume"]])
  assert_no_missing_values(value[["quote_volume"]])
  assert_integer(value[["trades"]])
  assert_no_missing_values(value[["trades"]])
  assert_double(value[["taker_buy_base_volume"]])
  assert_no_missing_values(value[["taker_buy_base_volume"]])
  assert_double(value[["taker_buy_quote_volume"]])
  assert_no_missing_values(value[["taker_buy_quote_volume"]])
  assert_character(value[["ignore"]])
  assert_no_missing_values(value[["ignore"]])
  return(value)
}

assert_args_BinanceFuturesData__get_mark_price <- function(symbol) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  return(invisible(NULL))
}

assert_return_BinanceFuturesData__get_mark_price <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "mark_price", "index_price", "estimated_settle_price", "last_funding_rate", "next_funding_time", "interest_rate", "time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["mark_price"]])
  assert_no_missing_values(value[["mark_price"]])
  assert_character(value[["index_price"]])
  assert_no_missing_values(value[["index_price"]])
  assert_character(value[["estimated_settle_price"]])
  assert_no_missing_values(value[["estimated_settle_price"]])
  assert_character(value[["last_funding_rate"]])
  assert_no_missing_values(value[["last_funding_rate"]])
  assert_datetime(value[["next_funding_time"]])
  assert_no_missing_values(value[["next_funding_time"]])
  assert_character(value[["interest_rate"]])
  assert_no_missing_values(value[["interest_rate"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  return(value)
}

assert_args_BinanceFuturesData__get_funding_rate <- function(symbol, start_time, end_time, limit) {
  assert_scalar_character(symbol)
  if (!is.null(start_time)) {
    assert_any_of(
      start_time,
      function(.x) {
        assert_scalar_datetime(.x)
      },
      function(.x) {
        assert_scalar_double(.x)
      }
    )
  }
  if (!is.null(end_time)) {
    assert_any_of(
      end_time,
      function(.x) {
        assert_scalar_datetime(.x)
      },
      function(.x) {
        assert_scalar_double(.x)
      }
    )
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  return(invisible(NULL))
}

assert_return_BinanceFuturesData__get_funding_rate <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "funding_rate", "funding_time", "mark_price"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["funding_rate"]])
  assert_no_missing_values(value[["funding_rate"]])
  assert_datetime(value[["funding_time"]])
  assert_no_missing_values(value[["funding_time"]])
  assert_character(value[["mark_price"]])
  assert_no_missing_values(value[["mark_price"]])
  return(value)
}

assert_args_BinanceFuturesData__get_24hr_stats <- function(symbol) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  return(invisible(NULL))
}

assert_return_BinanceFuturesData__get_24hr_stats <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "price_change", "price_change_percent", "weighted_avg_price", "last_price", "volume", "quote_volume", "open_time", "close_time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["price_change"]])
  assert_no_missing_values(value[["price_change"]])
  assert_character(value[["price_change_percent"]])
  assert_no_missing_values(value[["price_change_percent"]])
  assert_character(value[["weighted_avg_price"]])
  assert_no_missing_values(value[["weighted_avg_price"]])
  assert_character(value[["last_price"]])
  assert_no_missing_values(value[["last_price"]])
  assert_character(value[["volume"]])
  assert_no_missing_values(value[["volume"]])
  assert_character(value[["quote_volume"]])
  assert_no_missing_values(value[["quote_volume"]])
  assert_datetime(value[["open_time"]])
  assert_no_missing_values(value[["open_time"]])
  assert_datetime(value[["close_time"]])
  assert_no_missing_values(value[["close_time"]])
  return(value)
}

assert_args_BinanceFuturesData__get_ticker <- function(symbol) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  return(invisible(NULL))
}

assert_return_BinanceFuturesData__get_ticker <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "price", "time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  return(value)
}

assert_args_BinanceFuturesData__get_book_ticker <- function(symbol) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  return(invisible(NULL))
}

assert_return_BinanceFuturesData__get_book_ticker <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "bid_price", "bid_qty", "ask_price", "ask_qty"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["bid_price"]])
  assert_no_missing_values(value[["bid_price"]])
  assert_character(value[["bid_qty"]])
  assert_no_missing_values(value[["bid_qty"]])
  assert_character(value[["ask_price"]])
  assert_no_missing_values(value[["ask_price"]])
  assert_character(value[["ask_qty"]])
  assert_no_missing_values(value[["ask_qty"]])
  return(value)
}

assert_args_BinanceFuturesData__get_open_interest <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_BinanceFuturesData__get_open_interest <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "open_interest", "time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["open_interest"]])
  assert_no_missing_values(value[["open_interest"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  return(value)
}

assert_args_BinanceFuturesData__get_depth <- function(symbol, limit) {
  assert_scalar_character(symbol)
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  return(invisible(NULL))
}

assert_return_BinanceFuturesData__get_depth <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("last_update_id", "side", "price", "size"))
  assert_character(value[["last_update_id"]])
  assert_no_missing_values(value[["last_update_id"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_values_in_set(value[["side"]], c("bid", "ask"))
  assert_double(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_double(value[["size"]])
  assert_no_missing_values(value[["size"]])
  return(value)
}

assert_args_BinanceFuturesData__get_trades <- function(symbol, limit) {
  assert_scalar_character(symbol)
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  return(invisible(NULL))
}

assert_return_BinanceFuturesData__get_trades <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id", "price", "qty", "quote_qty", "time", "is_buyer_maker"))
  assert_double(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["qty"]])
  assert_no_missing_values(value[["qty"]])
  assert_character(value[["quote_qty"]])
  assert_no_missing_values(value[["quote_qty"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_logical(value[["is_buyer_maker"]])
  assert_no_missing_values(value[["is_buyer_maker"]])
  return(value)
}

assert_args_BinanceFuturesData__get_index_price_klines <- function(pair, interval, start_time, end_time, limit) {
  assert_scalar_character(pair)
  assert_scalar_character(interval)
  if (!is.null(start_time)) {
    assert_any_of(
      start_time,
      function(.x) {
        assert_scalar_datetime(.x)
      },
      function(.x) {
        assert_scalar_double(.x)
      }
    )
  }
  if (!is.null(end_time)) {
    assert_any_of(
      end_time,
      function(.x) {
        assert_scalar_datetime(.x)
      },
      function(.x) {
        assert_scalar_double(.x)
      }
    )
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  return(invisible(NULL))
}

assert_return_BinanceFuturesData__get_index_price_klines <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("datetime", "open", "high", "low", "close", "volume", "close_time", "quote_volume", "trades", "taker_buy_base_volume", "taker_buy_quote_volume", "ignore"))
  assert_datetime(value[["datetime"]])
  assert_no_missing_values(value[["datetime"]])
  assert_double(value[["open"]])
  assert_no_missing_values(value[["open"]])
  assert_double(value[["high"]])
  assert_no_missing_values(value[["high"]])
  assert_double(value[["low"]])
  assert_no_missing_values(value[["low"]])
  assert_double(value[["close"]])
  assert_no_missing_values(value[["close"]])
  assert_double(value[["volume"]])
  assert_no_missing_values(value[["volume"]])
  assert_datetime(value[["close_time"]])
  assert_no_missing_values(value[["close_time"]])
  assert_double(value[["quote_volume"]])
  assert_no_missing_values(value[["quote_volume"]])
  assert_integer(value[["trades"]])
  assert_no_missing_values(value[["trades"]])
  assert_double(value[["taker_buy_base_volume"]])
  assert_no_missing_values(value[["taker_buy_base_volume"]])
  assert_double(value[["taker_buy_quote_volume"]])
  assert_no_missing_values(value[["taker_buy_quote_volume"]])
  assert_character(value[["ignore"]])
  assert_no_missing_values(value[["ignore"]])
  return(value)
}

assert_args_BinanceFuturesData__get_mark_price_klines <- function(symbol, interval, start_time, end_time, limit) {
  assert_scalar_character(symbol)
  assert_scalar_character(interval)
  if (!is.null(start_time)) {
    assert_any_of(
      start_time,
      function(.x) {
        assert_scalar_datetime(.x)
      },
      function(.x) {
        assert_scalar_double(.x)
      }
    )
  }
  if (!is.null(end_time)) {
    assert_any_of(
      end_time,
      function(.x) {
        assert_scalar_datetime(.x)
      },
      function(.x) {
        assert_scalar_double(.x)
      }
    )
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  return(invisible(NULL))
}

assert_return_BinanceFuturesData__get_mark_price_klines <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("datetime", "open", "high", "low", "close", "volume", "close_time", "quote_volume", "trades", "taker_buy_base_volume", "taker_buy_quote_volume", "ignore"))
  assert_datetime(value[["datetime"]])
  assert_no_missing_values(value[["datetime"]])
  assert_double(value[["open"]])
  assert_no_missing_values(value[["open"]])
  assert_double(value[["high"]])
  assert_no_missing_values(value[["high"]])
  assert_double(value[["low"]])
  assert_no_missing_values(value[["low"]])
  assert_double(value[["close"]])
  assert_no_missing_values(value[["close"]])
  assert_double(value[["volume"]])
  assert_no_missing_values(value[["volume"]])
  assert_datetime(value[["close_time"]])
  assert_no_missing_values(value[["close_time"]])
  assert_double(value[["quote_volume"]])
  assert_no_missing_values(value[["quote_volume"]])
  assert_integer(value[["trades"]])
  assert_no_missing_values(value[["trades"]])
  assert_double(value[["taker_buy_base_volume"]])
  assert_no_missing_values(value[["taker_buy_base_volume"]])
  assert_double(value[["taker_buy_quote_volume"]])
  assert_no_missing_values(value[["taker_buy_quote_volume"]])
  assert_character(value[["ignore"]])
  assert_no_missing_values(value[["ignore"]])
  return(value)
}

assert_args_BinanceMargin__add_borrow <- function(asset, amount, is_isolated, symbol, recv_window) {
  assert_scalar_character(asset)
  assert_scalar_double(amount)
  assert_scalar_character(is_isolated)
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__add_borrow <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("tran_id"))
  assert_double(value[["tran_id"]])
  assert_no_missing_values(value[["tran_id"]])
  return(value)
}

assert_args_BinanceMargin__add_repay <- function(asset, amount, is_isolated, symbol, recv_window) {
  assert_scalar_character(asset)
  assert_scalar_double(amount)
  assert_scalar_character(is_isolated)
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__add_repay <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("tran_id"))
  assert_double(value[["tran_id"]])
  assert_no_missing_values(value[["tran_id"]])
  return(value)
}

assert_args_BinanceMargin__add_order <- function(symbol, side, type, quantity, quote_order_qty, price, stop_price, time_in_force, new_client_order_id, new_order_resp_type, side_effect_type, is_isolated, recv_window) {
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  assert_scalar_character(type)
  if (!is.null(quantity)) {
    assert_scalar_double(quantity)
  }
  if (!is.null(quote_order_qty)) {
    assert_scalar_double(quote_order_qty)
  }
  if (!is.null(price)) {
    assert_scalar_double(price)
  }
  if (!is.null(stop_price)) {
    assert_scalar_double(stop_price)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(new_client_order_id)) {
    assert_scalar_character(new_client_order_id)
  }
  if (!is.null(new_order_resp_type)) {
    assert_scalar_character(new_order_resp_type)
  }
  if (!is.null(side_effect_type)) {
    assert_scalar_character(side_effect_type)
  }
  if (!is.null(is_isolated)) {
    assert_scalar_character(is_isolated)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__add_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "client_order_id", "transact_time", "price", "orig_qty", "executed_qty", "status", "type", "side", "is_isolated"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_datetime(value[["transact_time"]])
  assert_no_missing_values(value[["transact_time"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_logical(value[["is_isolated"]])
  assert_no_missing_values(value[["is_isolated"]])
  return(value)
}

assert_args_BinanceMargin__cancel_order <- function(symbol, order_id, orig_client_order_id, is_isolated, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(order_id)) {
    assert_scalar_count(order_id)
  }
  if (!is.null(orig_client_order_id)) {
    assert_scalar_character(orig_client_order_id)
  }
  if (!is.null(is_isolated)) {
    assert_scalar_character(is_isolated)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__cancel_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "orig_client_order_id", "status", "transact_time", "is_isolated"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["orig_client_order_id"]])
  assert_no_missing_values(value[["orig_client_order_id"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_datetime(value[["transact_time"]])
  assert_no_missing_values(value[["transact_time"]])
  assert_logical(value[["is_isolated"]])
  assert_no_missing_values(value[["is_isolated"]])
  return(value)
}

assert_args_BinanceMargin__cancel_all_orders <- function(symbol, is_isolated, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(is_isolated)) {
    assert_scalar_character(is_isolated)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__cancel_all_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "orig_client_order_id", "status", "transact_time", "is_isolated"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["orig_client_order_id"]])
  assert_no_missing_values(value[["orig_client_order_id"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_datetime(value[["transact_time"]])
  assert_no_missing_values(value[["transact_time"]])
  assert_logical(value[["is_isolated"]])
  assert_no_missing_values(value[["is_isolated"]])
  return(value)
}

assert_args_BinanceMargin__get_order <- function(symbol, order_id, orig_client_order_id, is_isolated, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(order_id)) {
    assert_scalar_count(order_id)
  }
  if (!is.null(orig_client_order_id)) {
    assert_scalar_character(orig_client_order_id)
  }
  if (!is.null(is_isolated)) {
    assert_scalar_character(is_isolated)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__get_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "client_order_id", "price", "orig_qty", "executed_qty", "status", "type", "side", "time", "update_time", "is_isolated"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_datetime(value[["update_time"]])
  assert_no_missing_values(value[["update_time"]])
  assert_logical(value[["is_isolated"]])
  assert_no_missing_values(value[["is_isolated"]])
  return(value)
}

assert_args_BinanceMargin__get_open_orders <- function(symbol, is_isolated, recv_window) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  if (!is.null(is_isolated)) {
    assert_scalar_character(is_isolated)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__get_open_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "client_order_id", "price", "orig_qty", "executed_qty", "status", "type", "side", "time", "update_time", "is_isolated"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_datetime(value[["update_time"]])
  assert_no_missing_values(value[["update_time"]])
  assert_logical(value[["is_isolated"]])
  assert_no_missing_values(value[["is_isolated"]])
  return(value)
}

assert_args_BinanceMargin__get_all_orders <- function(symbol, order_id, start_time, end_time, limit, is_isolated, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(order_id)) {
    assert_scalar_count(order_id)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(is_isolated)) {
    assert_scalar_character(is_isolated)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__get_all_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "client_order_id", "price", "orig_qty", "executed_qty", "status", "type", "side", "time", "update_time", "is_isolated"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_datetime(value[["update_time"]])
  assert_no_missing_values(value[["update_time"]])
  assert_logical(value[["is_isolated"]])
  assert_no_missing_values(value[["is_isolated"]])
  return(value)
}

assert_args_BinanceMargin__get_account <- function(recv_window) {
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__get_account <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("borrow_enabled", "margin_level", "total_asset_of_btc", "total_liability_of_btc", "total_net_asset_of_btc", "trade_enabled", "transfer_enabled", "account_type"))
  assert_logical(value[["borrow_enabled"]])
  assert_no_missing_values(value[["borrow_enabled"]])
  assert_character(value[["margin_level"]])
  assert_no_missing_values(value[["margin_level"]])
  assert_character(value[["total_asset_of_btc"]])
  assert_no_missing_values(value[["total_asset_of_btc"]])
  assert_character(value[["total_liability_of_btc"]])
  assert_no_missing_values(value[["total_liability_of_btc"]])
  assert_character(value[["total_net_asset_of_btc"]])
  assert_no_missing_values(value[["total_net_asset_of_btc"]])
  assert_logical(value[["trade_enabled"]])
  assert_no_missing_values(value[["trade_enabled"]])
  assert_logical(value[["transfer_enabled"]])
  assert_no_missing_values(value[["transfer_enabled"]])
  assert_character(value[["account_type"]])
  assert_no_missing_values(value[["account_type"]])
  return(value)
}

assert_args_BinanceMargin__get_max_borrowable <- function(asset, isolated_symbol, recv_window) {
  assert_scalar_character(asset)
  if (!is.null(isolated_symbol)) {
    assert_scalar_character(isolated_symbol)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__get_max_borrowable <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("amount", "borrow_limit"))
  assert_character(value[["amount"]])
  assert_no_missing_values(value[["amount"]])
  assert_character(value[["borrow_limit"]])
  assert_no_missing_values(value[["borrow_limit"]])
  return(value)
}

assert_args_BinanceMargin__get_max_transferable <- function(asset, isolated_symbol, recv_window) {
  assert_scalar_character(asset)
  if (!is.null(isolated_symbol)) {
    assert_scalar_character(isolated_symbol)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__get_max_transferable <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("amount", "borrow_limit"))
  assert_character(value[["amount"]])
  assert_no_missing_values(value[["amount"]])
  assert_character(value[["borrow_limit"]])
  assert_no_missing_values(value[["borrow_limit"]])
  return(value)
}

assert_args_BinanceMargin__get_interest_history <- function(asset, start_time, end_time, current, size, archived, recv_window) {
  if (!is.null(asset)) {
    assert_scalar_character(asset)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(current)) {
    assert_scalar_count(current)
  }
  if (!is.null(size)) {
    assert_scalar_count(size)
  }
  if (!is.null(archived)) {
    assert_scalar_character(archived)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__get_interest_history <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("asset", "interest", "interest_accured_time", "interest_rate", "principal", "type", "isolated_symbol"))
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_character(value[["interest"]])
  assert_no_missing_values(value[["interest"]])
  assert_datetime(value[["interest_accured_time"]])
  assert_no_missing_values(value[["interest_accured_time"]])
  assert_character(value[["interest_rate"]])
  assert_no_missing_values(value[["interest_rate"]])
  assert_character(value[["principal"]])
  assert_no_missing_values(value[["principal"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["isolated_symbol"]])
  assert_no_missing_values(value[["isolated_symbol"]])
  return(value)
}

assert_args_BinanceMargin__get_force_liquidation_history <- function(start_time, end_time, isolated_symbol, current, size, recv_window) {
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(isolated_symbol)) {
    assert_scalar_character(isolated_symbol)
  }
  if (!is.null(current)) {
    assert_scalar_count(current)
  }
  if (!is.null(size)) {
    assert_scalar_count(size)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__get_force_liquidation_history <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("avg_price", "executed_qty", "order_id", "price", "qty", "side", "symbol", "time", "is_isolated", "updated_time"))
  assert_character(value[["avg_price"]])
  assert_no_missing_values(value[["avg_price"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["qty"]])
  assert_no_missing_values(value[["qty"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_logical(value[["is_isolated"]])
  assert_no_missing_values(value[["is_isolated"]])
  assert_datetime(value[["updated_time"]])
  assert_no_missing_values(value[["updated_time"]])
  return(value)
}

assert_args_BinanceMargin__get_trades <- function(symbol, order_id, start_time, end_time, from_id, limit, is_isolated, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(order_id)) {
    assert_scalar_count(order_id)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(from_id)) {
    assert_scalar_count(from_id)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(is_isolated)) {
    assert_scalar_character(is_isolated)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__get_trades <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "id", "order_id", "price", "qty", "commission", "commission_asset", "time", "is_buyer", "is_maker", "is_isolated"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["qty"]])
  assert_no_missing_values(value[["qty"]])
  assert_character(value[["commission"]])
  assert_no_missing_values(value[["commission"]])
  assert_character(value[["commission_asset"]])
  assert_no_missing_values(value[["commission_asset"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_logical(value[["is_buyer"]])
  assert_no_missing_values(value[["is_buyer"]])
  assert_logical(value[["is_maker"]])
  assert_no_missing_values(value[["is_maker"]])
  assert_logical(value[["is_isolated"]])
  assert_no_missing_values(value[["is_isolated"]])
  return(value)
}

assert_args_BinanceMargin__get_isolated_account <- function(symbols, recv_window) {
  if (!is.null(symbols)) {
    assert_scalar_character(symbols)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__get_isolated_account <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("total_asset_of_btc", "total_liability_of_btc", "total_net_asset_of_btc", "base_asset", "quote_asset", "symbol", "isolated_created", "enabled", "margin_level", "trade_enabled"))
  assert_character(value[["total_asset_of_btc"]])
  assert_no_missing_values(value[["total_asset_of_btc"]])
  assert_character(value[["total_liability_of_btc"]])
  assert_no_missing_values(value[["total_liability_of_btc"]])
  assert_character(value[["total_net_asset_of_btc"]])
  assert_no_missing_values(value[["total_net_asset_of_btc"]])
  assert_list(value[["base_asset"]])
  assert_list(value[["quote_asset"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_logical(value[["isolated_created"]])
  assert_no_missing_values(value[["isolated_created"]])
  assert_logical(value[["enabled"]])
  assert_no_missing_values(value[["enabled"]])
  assert_character(value[["margin_level"]])
  assert_no_missing_values(value[["margin_level"]])
  assert_logical(value[["trade_enabled"]])
  assert_no_missing_values(value[["trade_enabled"]])
  return(value)
}

assert_args_BinanceMargin__add_isolated_transfer <- function(asset, symbol, trans_from, trans_to, amount, recv_window) {
  assert_scalar_character(asset)
  assert_scalar_character(symbol)
  assert_scalar_character(trans_from)
  assert_scalar_character(trans_to)
  assert_scalar_double(amount)
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMargin__add_isolated_transfer <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("tran_id"))
  assert_double(value[["tran_id"]])
  assert_no_missing_values(value[["tran_id"]])
  return(value)
}

assert_args_BinanceMarginData__get_all_pairs <- function(recv_window) {
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMarginData__get_all_pairs <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("base", "id", "is_buy_allowed", "is_margin_trade", "is_sell_allowed", "quote", "symbol"))
  assert_character(value[["base"]])
  assert_no_missing_values(value[["base"]])
  assert_double(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_logical(value[["is_buy_allowed"]])
  assert_no_missing_values(value[["is_buy_allowed"]])
  assert_logical(value[["is_margin_trade"]])
  assert_no_missing_values(value[["is_margin_trade"]])
  assert_logical(value[["is_sell_allowed"]])
  assert_no_missing_values(value[["is_sell_allowed"]])
  assert_character(value[["quote"]])
  assert_no_missing_values(value[["quote"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  return(value)
}

assert_args_BinanceMarginData__get_isolated_pairs <- function(recv_window) {
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMarginData__get_isolated_pairs <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "base", "quote", "is_margin_trade", "is_buy_allowed", "is_sell_allowed"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["base"]])
  assert_no_missing_values(value[["base"]])
  assert_character(value[["quote"]])
  assert_no_missing_values(value[["quote"]])
  assert_logical(value[["is_margin_trade"]])
  assert_no_missing_values(value[["is_margin_trade"]])
  assert_logical(value[["is_buy_allowed"]])
  assert_no_missing_values(value[["is_buy_allowed"]])
  assert_logical(value[["is_sell_allowed"]])
  assert_no_missing_values(value[["is_sell_allowed"]])
  return(value)
}

assert_args_BinanceMarginData__get_price_index <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_BinanceMarginData__get_price_index <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("calc_time", "price", "symbol"))
  assert_datetime(value[["calc_time"]])
  assert_no_missing_values(value[["calc_time"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  return(value)
}

assert_args_BinanceMarginData__get_interest_rate_history <- function(asset, vip_level, start_time, end_time, recv_window) {
  assert_scalar_character(asset)
  if (!is.null(vip_level)) {
    assert_scalar_count(vip_level)
  }
  if (!is.null(start_time)) {
    assert_scalar_double(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_double(end_time)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMarginData__get_interest_rate_history <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("asset", "daily_interest_rate", "timestamp", "vip_level"))
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_character(value[["daily_interest_rate"]])
  assert_no_missing_values(value[["daily_interest_rate"]])
  assert_datetime(value[["timestamp"]])
  assert_no_missing_values(value[["timestamp"]])
  assert_integer(value[["vip_level"]])
  assert_no_missing_values(value[["vip_level"]])
  return(value)
}

assert_args_BinanceMarginData__get_cross_margin_data <- function(vip_level, coin, recv_window) {
  if (!is.null(vip_level)) {
    assert_scalar_count(vip_level)
  }
  if (!is.null(coin)) {
    assert_scalar_character(coin)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMarginData__get_cross_margin_data <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("vip_level", "coin", "transfer_in", "transfer_out", "borrowable", "daily_interest", "yearly_interest", "marginable_pair"))
  assert_integer(value[["vip_level"]])
  assert_no_missing_values(value[["vip_level"]])
  assert_character(value[["coin"]])
  assert_no_missing_values(value[["coin"]])
  assert_logical(value[["transfer_in"]])
  assert_no_missing_values(value[["transfer_in"]])
  assert_logical(value[["transfer_out"]])
  assert_no_missing_values(value[["transfer_out"]])
  assert_logical(value[["borrowable"]])
  assert_no_missing_values(value[["borrowable"]])
  assert_character(value[["daily_interest"]])
  assert_no_missing_values(value[["daily_interest"]])
  assert_character(value[["yearly_interest"]])
  assert_no_missing_values(value[["yearly_interest"]])
  assert_character(value[["marginable_pair"]])
  assert_no_missing_values(value[["marginable_pair"]])
  return(value)
}

assert_args_BinanceMarginData__get_isolated_margin_data <- function(vip_level, symbol, recv_window) {
  if (!is.null(vip_level)) {
    assert_scalar_count(vip_level)
  }
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceMarginData__get_isolated_margin_data <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("vip_level", "symbol", "leverage", "data"))
  assert_integer(value[["vip_level"]])
  assert_no_missing_values(value[["vip_level"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["leverage"]])
  assert_no_missing_values(value[["leverage"]])
  assert_list(value[["data"]])
  return(value)
}

assert_return_BinanceMarketData__get_server_time <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("server_time"))
  assert_datetime(value[["server_time"]])
  assert_no_missing_values(value[["server_time"]])
  return(value)
}

assert_args_BinanceMarketData__get_exchange_info <- function(symbol, symbols) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  if (!is.null(symbols)) {
    assert_character(symbols)
    assert_no_missing_values(symbols)
  }
  return(invisible(NULL))
}

assert_return_BinanceMarketData__get_exchange_info <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "status", "base_asset", "base_asset_precision", "quote_asset", "quote_asset_precision", "quote_precision", "order_types", "iceberg_allowed", "oco_allowed", "oto_allowed", "quote_order_qty_market_allowed", "allow_trailing_stop", "cancel_replace_allowed", "is_spot_trading_allowed", "is_margin_trading_allowed", "lot_min_qty", "lot_max_qty", "lot_step_size", "price_min", "price_max", "price_tick_size", "min_notional", "filters_raw", "permissions", "permission_sets", "default_self_trade_prevention_mode", "allowed_self_trade_prevention_modes"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["base_asset"]])
  assert_no_missing_values(value[["base_asset"]])
  assert_integer(value[["base_asset_precision"]])
  assert_no_missing_values(value[["base_asset_precision"]])
  assert_character(value[["quote_asset"]])
  assert_no_missing_values(value[["quote_asset"]])
  assert_integer(value[["quote_asset_precision"]])
  assert_no_missing_values(value[["quote_asset_precision"]])
  assert_integer(value[["quote_precision"]])
  assert_no_missing_values(value[["quote_precision"]])
  assert_character(value[["order_types"]])
  assert_no_missing_values(value[["order_types"]])
  assert_logical(value[["iceberg_allowed"]])
  assert_no_missing_values(value[["iceberg_allowed"]])
  assert_logical(value[["oco_allowed"]])
  assert_no_missing_values(value[["oco_allowed"]])
  assert_logical(value[["oto_allowed"]])
  assert_no_missing_values(value[["oto_allowed"]])
  assert_logical(value[["quote_order_qty_market_allowed"]])
  assert_no_missing_values(value[["quote_order_qty_market_allowed"]])
  assert_logical(value[["allow_trailing_stop"]])
  assert_no_missing_values(value[["allow_trailing_stop"]])
  assert_logical(value[["cancel_replace_allowed"]])
  assert_no_missing_values(value[["cancel_replace_allowed"]])
  assert_logical(value[["is_spot_trading_allowed"]])
  assert_no_missing_values(value[["is_spot_trading_allowed"]])
  assert_logical(value[["is_margin_trading_allowed"]])
  assert_no_missing_values(value[["is_margin_trading_allowed"]])
  assert_double(value[["lot_min_qty"]])
  assert_double(value[["lot_max_qty"]])
  assert_double(value[["lot_step_size"]])
  assert_double(value[["price_min"]])
  assert_no_missing_values(value[["price_min"]])
  assert_double(value[["price_max"]])
  assert_no_missing_values(value[["price_max"]])
  assert_double(value[["price_tick_size"]])
  assert_no_missing_values(value[["price_tick_size"]])
  assert_double(value[["min_notional"]])
  assert_character(value[["filters_raw"]])
  assert_no_missing_values(value[["filters_raw"]])
  assert_character(value[["permissions"]])
  assert_character(value[["permission_sets"]])
  assert_character(value[["default_self_trade_prevention_mode"]])
  assert_no_missing_values(value[["default_self_trade_prevention_mode"]])
  assert_character(value[["allowed_self_trade_prevention_modes"]])
  assert_no_missing_values(value[["allowed_self_trade_prevention_modes"]])
  return(value)
}

assert_return_BinanceMarketData__get_rate_limits <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("rate_limit_type", "interval", "interval_num", "limit"))
  assert_character(value[["rate_limit_type"]])
  assert_no_missing_values(value[["rate_limit_type"]])
  assert_character(value[["interval"]])
  assert_no_missing_values(value[["interval"]])
  assert_integer(value[["interval_num"]])
  assert_no_missing_values(value[["interval_num"]])
  assert_integer(value[["limit"]])
  assert_no_missing_values(value[["limit"]])
  return(value)
}

assert_return_BinanceMarketData__get_exchange_filters <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_BinanceMarketData__get_ticker <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_BinanceMarketData__get_ticker <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "price"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  return(value)
}

assert_return_BinanceMarketData__get_all_tickers <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "price"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  return(value)
}

assert_args_BinanceMarketData__get_book_ticker <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_BinanceMarketData__get_book_ticker <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "bid_price", "bid_qty", "ask_price", "ask_qty"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["bid_price"]])
  assert_no_missing_values(value[["bid_price"]])
  assert_character(value[["bid_qty"]])
  assert_no_missing_values(value[["bid_qty"]])
  assert_character(value[["ask_price"]])
  assert_no_missing_values(value[["ask_price"]])
  assert_character(value[["ask_qty"]])
  assert_no_missing_values(value[["ask_qty"]])
  return(value)
}

assert_args_BinanceMarketData__get_24hr_stats <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_BinanceMarketData__get_24hr_stats <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "price_change", "price_change_percent", "weighted_avg_price", "prev_close_price", "last_price", "last_qty", "bid_price", "bid_qty", "ask_price", "ask_qty", "open_price", "high_price", "low_price", "volume", "quote_volume", "open_time", "close_time", "first_id", "last_id", "count"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["price_change"]])
  assert_no_missing_values(value[["price_change"]])
  assert_character(value[["price_change_percent"]])
  assert_no_missing_values(value[["price_change_percent"]])
  assert_character(value[["weighted_avg_price"]])
  assert_no_missing_values(value[["weighted_avg_price"]])
  assert_character(value[["prev_close_price"]])
  assert_no_missing_values(value[["prev_close_price"]])
  assert_character(value[["last_price"]])
  assert_no_missing_values(value[["last_price"]])
  assert_character(value[["last_qty"]])
  assert_no_missing_values(value[["last_qty"]])
  assert_character(value[["bid_price"]])
  assert_no_missing_values(value[["bid_price"]])
  assert_character(value[["bid_qty"]])
  assert_no_missing_values(value[["bid_qty"]])
  assert_character(value[["ask_price"]])
  assert_no_missing_values(value[["ask_price"]])
  assert_character(value[["ask_qty"]])
  assert_no_missing_values(value[["ask_qty"]])
  assert_character(value[["open_price"]])
  assert_no_missing_values(value[["open_price"]])
  assert_character(value[["high_price"]])
  assert_no_missing_values(value[["high_price"]])
  assert_character(value[["low_price"]])
  assert_no_missing_values(value[["low_price"]])
  assert_character(value[["volume"]])
  assert_no_missing_values(value[["volume"]])
  assert_character(value[["quote_volume"]])
  assert_no_missing_values(value[["quote_volume"]])
  assert_datetime(value[["open_time"]])
  assert_no_missing_values(value[["open_time"]])
  assert_datetime(value[["close_time"]])
  assert_no_missing_values(value[["close_time"]])
  assert_double(value[["first_id"]])
  assert_no_missing_values(value[["first_id"]])
  assert_double(value[["last_id"]])
  assert_no_missing_values(value[["last_id"]])
  assert_integer(value[["count"]])
  assert_no_missing_values(value[["count"]])
  return(value)
}

assert_return_BinanceMarketData__get_all_24hr_stats <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "price_change", "price_change_percent", "weighted_avg_price", "prev_close_price", "last_price", "last_qty", "bid_price", "bid_qty", "ask_price", "ask_qty", "open_price", "high_price", "low_price", "volume", "quote_volume", "open_time", "close_time", "first_id", "last_id", "count"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["price_change"]])
  assert_no_missing_values(value[["price_change"]])
  assert_character(value[["price_change_percent"]])
  assert_no_missing_values(value[["price_change_percent"]])
  assert_character(value[["weighted_avg_price"]])
  assert_no_missing_values(value[["weighted_avg_price"]])
  assert_character(value[["prev_close_price"]])
  assert_no_missing_values(value[["prev_close_price"]])
  assert_character(value[["last_price"]])
  assert_no_missing_values(value[["last_price"]])
  assert_character(value[["last_qty"]])
  assert_no_missing_values(value[["last_qty"]])
  assert_character(value[["bid_price"]])
  assert_no_missing_values(value[["bid_price"]])
  assert_character(value[["bid_qty"]])
  assert_no_missing_values(value[["bid_qty"]])
  assert_character(value[["ask_price"]])
  assert_no_missing_values(value[["ask_price"]])
  assert_character(value[["ask_qty"]])
  assert_no_missing_values(value[["ask_qty"]])
  assert_character(value[["open_price"]])
  assert_no_missing_values(value[["open_price"]])
  assert_character(value[["high_price"]])
  assert_no_missing_values(value[["high_price"]])
  assert_character(value[["low_price"]])
  assert_no_missing_values(value[["low_price"]])
  assert_character(value[["volume"]])
  assert_no_missing_values(value[["volume"]])
  assert_character(value[["quote_volume"]])
  assert_no_missing_values(value[["quote_volume"]])
  assert_datetime(value[["open_time"]])
  assert_no_missing_values(value[["open_time"]])
  assert_datetime(value[["close_time"]])
  assert_no_missing_values(value[["close_time"]])
  assert_double(value[["first_id"]])
  assert_no_missing_values(value[["first_id"]])
  assert_double(value[["last_id"]])
  assert_no_missing_values(value[["last_id"]])
  assert_integer(value[["count"]])
  assert_no_missing_values(value[["count"]])
  return(value)
}

assert_args_BinanceMarketData__get_avg_price <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_BinanceMarketData__get_avg_price <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("mins", "price", "close_time"))
  assert_integer(value[["mins"]])
  assert_no_missing_values(value[["mins"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_datetime(value[["close_time"]])
  assert_no_missing_values(value[["close_time"]])
  return(value)
}

assert_args_BinanceMarketData__get_depth <- function(symbol, limit) {
  assert_scalar_character(symbol)
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  return(invisible(NULL))
}

assert_return_BinanceMarketData__get_depth <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("last_update_id", "side", "price", "size"))
  assert_character(value[["last_update_id"]])
  assert_no_missing_values(value[["last_update_id"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_values_in_set(value[["side"]], c("bid", "ask"))
  assert_double(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_double(value[["size"]])
  assert_no_missing_values(value[["size"]])
  return(value)
}

assert_args_BinanceMarketData__get_trades <- function(symbol, limit) {
  assert_scalar_character(symbol)
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  return(invisible(NULL))
}

assert_return_BinanceMarketData__get_trades <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id", "price", "qty", "quote_qty", "time", "is_buyer_maker", "is_best_match"))
  assert_double(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["qty"]])
  assert_no_missing_values(value[["qty"]])
  assert_character(value[["quote_qty"]])
  assert_no_missing_values(value[["quote_qty"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_logical(value[["is_buyer_maker"]])
  assert_no_missing_values(value[["is_buyer_maker"]])
  assert_logical(value[["is_best_match"]])
  assert_no_missing_values(value[["is_best_match"]])
  return(value)
}

assert_args_BinanceMarketData__get_klines <- function(symbol, interval, start_time, end_time, limit, fetch_all, sleep, on_page) {
  assert_scalar_character(symbol)
  assert_scalar_character(interval)
  if (!is.null(start_time)) {
    assert_any_of(
      start_time,
      function(.x) {
        assert_scalar_datetime(.x)
      },
      function(.x) {
        assert_scalar_double(.x)
      }
    )
  }
  if (!is.null(end_time)) {
    assert_any_of(
      end_time,
      function(.x) {
        assert_scalar_datetime(.x)
      },
      function(.x) {
        assert_scalar_double(.x)
      }
    )
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  assert_scalar_logical(fetch_all)
  assert_scalar_double(sleep)
  if (!is.null(on_page)) {
    assert_function(on_page)
  }
  return(invisible(NULL))
}

assert_return_BinanceMarketData__get_klines <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("datetime", "open", "high", "low", "close", "volume", "close_time", "quote_volume", "trades", "taker_buy_base_volume", "taker_buy_quote_volume", "ignore"))
  assert_datetime(value[["datetime"]])
  assert_no_missing_values(value[["datetime"]])
  assert_double(value[["open"]])
  assert_no_missing_values(value[["open"]])
  assert_double(value[["high"]])
  assert_no_missing_values(value[["high"]])
  assert_double(value[["low"]])
  assert_no_missing_values(value[["low"]])
  assert_double(value[["close"]])
  assert_no_missing_values(value[["close"]])
  assert_double(value[["volume"]])
  assert_no_missing_values(value[["volume"]])
  assert_datetime(value[["close_time"]])
  assert_no_missing_values(value[["close_time"]])
  assert_double(value[["quote_volume"]])
  assert_no_missing_values(value[["quote_volume"]])
  assert_integer(value[["trades"]])
  assert_no_missing_values(value[["trades"]])
  assert_double(value[["taker_buy_base_volume"]])
  assert_no_missing_values(value[["taker_buy_base_volume"]])
  assert_double(value[["taker_buy_quote_volume"]])
  assert_no_missing_values(value[["taker_buy_quote_volume"]])
  assert_character(value[["ignore"]])
  assert_no_missing_values(value[["ignore"]])
  return(value)
}

assert_args_BinanceMarketStream__depth <- function(symbol, speed, handler) {
  assert_scalar_character(symbol)
  assert_scalar_character(speed)
  assert_value_in_set(speed, c("1000ms", "100ms"))
  if (!is.null(handler)) {
    assert_function(handler)
  }
  return(invisible(NULL))
}

assert_return_BinanceMarketStream__depth <- function(value) {
  assert_class(value, "BinanceMarketStream")
  return(value)
}

assert_args_BinanceOcoOrders__add_oco_order <- function(symbol, side, quantity, price, stop_price, stop_limit_price, stop_limit_time_in_force, list_client_order_id, limit_client_order_id, stop_client_order_id, limit_iceberg_qty, stop_iceberg_qty, new_order_resp_type, self_trade_prevention_mode, recv_window) {
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  assert_scalar_double(quantity)
  assert_scalar_double(price)
  assert_scalar_double(stop_price)
  if (!is.null(stop_limit_price)) {
    assert_scalar_double(stop_limit_price)
  }
  if (!is.null(stop_limit_time_in_force)) {
    assert_scalar_character(stop_limit_time_in_force)
  }
  if (!is.null(list_client_order_id)) {
    assert_scalar_character(list_client_order_id)
  }
  if (!is.null(limit_client_order_id)) {
    assert_scalar_character(limit_client_order_id)
  }
  if (!is.null(stop_client_order_id)) {
    assert_scalar_character(stop_client_order_id)
  }
  if (!is.null(limit_iceberg_qty)) {
    assert_scalar_double(limit_iceberg_qty)
  }
  if (!is.null(stop_iceberg_qty)) {
    assert_scalar_double(stop_iceberg_qty)
  }
  if (!is.null(new_order_resp_type)) {
    assert_scalar_character(new_order_resp_type)
  }
  if (!is.null(self_trade_prevention_mode)) {
    assert_scalar_character(self_trade_prevention_mode)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceOcoOrders__add_oco_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_list_id", "contingency_type", "list_status_type", "list_order_status", "list_client_order_id", "transact_time", "symbol", "order_report_symbol", "order_report_order_id", "order_report_order_list_id", "order_report_client_order_id", "order_report_transact_time", "order_report_price", "order_report_orig_qty", "order_report_executed_qty", "order_report_cummulative_quote_qty", "order_report_status", "order_report_time_in_force", "order_report_type", "order_report_side", "order_report_stop_price", "order_report_self_trade_prevention_mode"))
  assert_double(value[["order_list_id"]])
  assert_no_missing_values(value[["order_list_id"]])
  assert_character(value[["contingency_type"]])
  assert_no_missing_values(value[["contingency_type"]])
  assert_character(value[["list_status_type"]])
  assert_no_missing_values(value[["list_status_type"]])
  assert_character(value[["list_order_status"]])
  assert_no_missing_values(value[["list_order_status"]])
  assert_character(value[["list_client_order_id"]])
  assert_no_missing_values(value[["list_client_order_id"]])
  assert_datetime(value[["transact_time"]])
  assert_no_missing_values(value[["transact_time"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["order_report_symbol"]])
  assert_no_missing_values(value[["order_report_symbol"]])
  assert_double(value[["order_report_order_id"]])
  assert_no_missing_values(value[["order_report_order_id"]])
  assert_double(value[["order_report_order_list_id"]])
  assert_no_missing_values(value[["order_report_order_list_id"]])
  assert_character(value[["order_report_client_order_id"]])
  assert_no_missing_values(value[["order_report_client_order_id"]])
  assert_datetime(value[["order_report_transact_time"]])
  assert_no_missing_values(value[["order_report_transact_time"]])
  assert_character(value[["order_report_price"]])
  assert_no_missing_values(value[["order_report_price"]])
  assert_character(value[["order_report_orig_qty"]])
  assert_no_missing_values(value[["order_report_orig_qty"]])
  assert_character(value[["order_report_executed_qty"]])
  assert_no_missing_values(value[["order_report_executed_qty"]])
  assert_character(value[["order_report_cummulative_quote_qty"]])
  assert_no_missing_values(value[["order_report_cummulative_quote_qty"]])
  assert_character(value[["order_report_status"]])
  assert_no_missing_values(value[["order_report_status"]])
  assert_character(value[["order_report_time_in_force"]])
  assert_no_missing_values(value[["order_report_time_in_force"]])
  assert_character(value[["order_report_type"]])
  assert_no_missing_values(value[["order_report_type"]])
  assert_character(value[["order_report_side"]])
  assert_no_missing_values(value[["order_report_side"]])
  assert_character(value[["order_report_stop_price"]])
  assert_character(value[["order_report_self_trade_prevention_mode"]])
  assert_no_missing_values(value[["order_report_self_trade_prevention_mode"]])
  return(value)
}

assert_args_BinanceOcoOrders__cancel_oco_order <- function(symbol, order_list_id, list_client_order_id, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(order_list_id)) {
    assert_scalar_count(order_list_id)
  }
  if (!is.null(list_client_order_id)) {
    assert_scalar_character(list_client_order_id)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceOcoOrders__cancel_oco_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_list_id", "contingency_type", "list_status_type", "list_order_status", "list_client_order_id", "transact_time", "symbol", "order_report_symbol", "order_report_order_id", "order_report_order_list_id", "order_report_client_order_id", "order_report_transact_time", "order_report_price", "order_report_orig_qty", "order_report_executed_qty", "order_report_cummulative_quote_qty", "order_report_status", "order_report_time_in_force", "order_report_type", "order_report_side", "order_report_stop_price", "order_report_self_trade_prevention_mode"))
  assert_double(value[["order_list_id"]])
  assert_no_missing_values(value[["order_list_id"]])
  assert_character(value[["contingency_type"]])
  assert_no_missing_values(value[["contingency_type"]])
  assert_character(value[["list_status_type"]])
  assert_no_missing_values(value[["list_status_type"]])
  assert_character(value[["list_order_status"]])
  assert_no_missing_values(value[["list_order_status"]])
  assert_character(value[["list_client_order_id"]])
  assert_no_missing_values(value[["list_client_order_id"]])
  assert_datetime(value[["transact_time"]])
  assert_no_missing_values(value[["transact_time"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["order_report_symbol"]])
  assert_no_missing_values(value[["order_report_symbol"]])
  assert_double(value[["order_report_order_id"]])
  assert_no_missing_values(value[["order_report_order_id"]])
  assert_double(value[["order_report_order_list_id"]])
  assert_no_missing_values(value[["order_report_order_list_id"]])
  assert_character(value[["order_report_client_order_id"]])
  assert_no_missing_values(value[["order_report_client_order_id"]])
  assert_datetime(value[["order_report_transact_time"]])
  assert_no_missing_values(value[["order_report_transact_time"]])
  assert_character(value[["order_report_price"]])
  assert_no_missing_values(value[["order_report_price"]])
  assert_character(value[["order_report_orig_qty"]])
  assert_no_missing_values(value[["order_report_orig_qty"]])
  assert_character(value[["order_report_executed_qty"]])
  assert_no_missing_values(value[["order_report_executed_qty"]])
  assert_character(value[["order_report_cummulative_quote_qty"]])
  assert_no_missing_values(value[["order_report_cummulative_quote_qty"]])
  assert_character(value[["order_report_status"]])
  assert_no_missing_values(value[["order_report_status"]])
  assert_character(value[["order_report_time_in_force"]])
  assert_no_missing_values(value[["order_report_time_in_force"]])
  assert_character(value[["order_report_type"]])
  assert_no_missing_values(value[["order_report_type"]])
  assert_character(value[["order_report_side"]])
  assert_no_missing_values(value[["order_report_side"]])
  assert_character(value[["order_report_stop_price"]])
  assert_character(value[["order_report_self_trade_prevention_mode"]])
  assert_no_missing_values(value[["order_report_self_trade_prevention_mode"]])
  return(value)
}

assert_args_BinanceOcoOrders__get_oco_order <- function(order_list_id, orig_client_order_id, recv_window) {
  if (!is.null(order_list_id)) {
    assert_scalar_count(order_list_id)
  }
  if (!is.null(orig_client_order_id)) {
    assert_scalar_character(orig_client_order_id)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceOcoOrders__get_oco_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_list_id", "contingency_type", "list_status_type", "list_order_status", "list_client_order_id", "transaction_time", "symbol", "order_symbol", "order_order_id", "order_client_order_id"))
  assert_double(value[["order_list_id"]])
  assert_no_missing_values(value[["order_list_id"]])
  assert_character(value[["contingency_type"]])
  assert_no_missing_values(value[["contingency_type"]])
  assert_character(value[["list_status_type"]])
  assert_no_missing_values(value[["list_status_type"]])
  assert_character(value[["list_order_status"]])
  assert_no_missing_values(value[["list_order_status"]])
  assert_character(value[["list_client_order_id"]])
  assert_no_missing_values(value[["list_client_order_id"]])
  assert_datetime(value[["transaction_time"]])
  assert_no_missing_values(value[["transaction_time"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["order_symbol"]])
  assert_no_missing_values(value[["order_symbol"]])
  assert_double(value[["order_order_id"]])
  assert_no_missing_values(value[["order_order_id"]])
  assert_character(value[["order_client_order_id"]])
  assert_no_missing_values(value[["order_client_order_id"]])
  return(value)
}

assert_args_BinanceOcoOrders__get_open_oco_orders <- function(recv_window) {
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceOcoOrders__get_open_oco_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_list_id", "contingency_type", "list_status_type", "list_order_status", "list_client_order_id", "transaction_time", "symbol", "order_symbol", "order_order_id", "order_client_order_id"))
  assert_double(value[["order_list_id"]])
  assert_no_missing_values(value[["order_list_id"]])
  assert_character(value[["contingency_type"]])
  assert_no_missing_values(value[["contingency_type"]])
  assert_character(value[["list_status_type"]])
  assert_no_missing_values(value[["list_status_type"]])
  assert_character(value[["list_order_status"]])
  assert_no_missing_values(value[["list_order_status"]])
  assert_character(value[["list_client_order_id"]])
  assert_no_missing_values(value[["list_client_order_id"]])
  assert_datetime(value[["transaction_time"]])
  assert_no_missing_values(value[["transaction_time"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["order_symbol"]])
  assert_no_missing_values(value[["order_symbol"]])
  assert_double(value[["order_order_id"]])
  assert_no_missing_values(value[["order_order_id"]])
  assert_character(value[["order_client_order_id"]])
  assert_no_missing_values(value[["order_client_order_id"]])
  return(value)
}

assert_args_BinanceOcoOrders__get_all_oco_orders <- function(from_id, start_time, end_time, limit, recv_window) {
  if (!is.null(from_id)) {
    assert_scalar_count(from_id)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceOcoOrders__get_all_oco_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_list_id", "contingency_type", "list_status_type", "list_order_status", "list_client_order_id", "transaction_time", "symbol", "order_symbol", "order_order_id", "order_client_order_id"))
  assert_double(value[["order_list_id"]])
  assert_no_missing_values(value[["order_list_id"]])
  assert_character(value[["contingency_type"]])
  assert_no_missing_values(value[["contingency_type"]])
  assert_character(value[["list_status_type"]])
  assert_no_missing_values(value[["list_status_type"]])
  assert_character(value[["list_order_status"]])
  assert_no_missing_values(value[["list_order_status"]])
  assert_character(value[["list_client_order_id"]])
  assert_no_missing_values(value[["list_client_order_id"]])
  assert_datetime(value[["transaction_time"]])
  assert_no_missing_values(value[["transaction_time"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["order_symbol"]])
  assert_no_missing_values(value[["order_symbol"]])
  assert_double(value[["order_order_id"]])
  assert_no_missing_values(value[["order_order_id"]])
  assert_character(value[["order_client_order_id"]])
  assert_no_missing_values(value[["order_client_order_id"]])
  return(value)
}

assert_args_BinanceSubAccount__add_sub_account <- function(sub_account_string, recv_window) {
  assert_scalar_character(sub_account_string)
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceSubAccount__add_sub_account <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("email"))
  assert_character(value[["email"]])
  assert_no_missing_values(value[["email"]])
  return(value)
}

assert_args_BinanceSubAccount__get_sub_accounts <- function(email, is_freeze, page, limit, recv_window) {
  if (!is.null(email)) {
    assert_scalar_character(email)
  }
  if (!is.null(is_freeze)) {
    assert_scalar_logical(is_freeze)
  }
  if (!is.null(page)) {
    assert_scalar_count(page)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceSubAccount__get_sub_accounts <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("email", "is_freeze", "create_time", "is_managed_sub_account", "is_asset_management_sub_account"))
  assert_character(value[["email"]])
  assert_no_missing_values(value[["email"]])
  assert_logical(value[["is_freeze"]])
  assert_no_missing_values(value[["is_freeze"]])
  assert_datetime(value[["create_time"]])
  assert_no_missing_values(value[["create_time"]])
  assert_logical(value[["is_managed_sub_account"]])
  assert_no_missing_values(value[["is_managed_sub_account"]])
  assert_logical(value[["is_asset_management_sub_account"]])
  assert_no_missing_values(value[["is_asset_management_sub_account"]])
  return(value)
}

assert_args_BinanceSubAccount__get_balances <- function(email, recv_window) {
  assert_scalar_character(email)
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceSubAccount__get_balances <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("asset", "free", "locked"))
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_double(value[["free"]])
  assert_no_missing_values(value[["free"]])
  assert_double(value[["locked"]])
  assert_no_missing_values(value[["locked"]])
  return(value)
}

assert_args_BinanceSubAccount__get_spot_summary <- function(email, page, size, recv_window) {
  if (!is.null(email)) {
    assert_scalar_character(email)
  }
  if (!is.null(page)) {
    assert_scalar_count(page)
  }
  if (!is.null(size)) {
    assert_scalar_count(size)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceSubAccount__get_spot_summary <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("total_count", "master_account_total_asset", "sub_user_email", "sub_user_total_asset"))
  assert_integer(value[["total_count"]])
  assert_no_missing_values(value[["total_count"]])
  assert_character(value[["master_account_total_asset"]])
  assert_no_missing_values(value[["master_account_total_asset"]])
  assert_character(value[["sub_user_email"]])
  assert_no_missing_values(value[["sub_user_email"]])
  assert_character(value[["sub_user_total_asset"]])
  assert_no_missing_values(value[["sub_user_total_asset"]])
  return(value)
}

assert_args_BinanceSubAccount__add_transfer <- function(from_email, to_email, from_account_type, to_account_type, asset, amount, client_tran_id, recv_window) {
  if (!is.null(from_email)) {
    assert_scalar_character(from_email)
  }
  if (!is.null(to_email)) {
    assert_scalar_character(to_email)
  }
  assert_scalar_character(from_account_type)
  assert_scalar_character(to_account_type)
  assert_scalar_character(asset)
  assert_scalar_double(amount)
  if (!is.null(client_tran_id)) {
    assert_scalar_character(client_tran_id)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceSubAccount__add_transfer <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("tran_id", "client_tran_id"))
  assert_double(value[["tran_id"]])
  assert_no_missing_values(value[["tran_id"]])
  assert_character(value[["client_tran_id"]])
  assert_no_missing_values(value[["client_tran_id"]])
  return(value)
}

assert_args_BinanceSubAccount__get_transfer_history <- function(from_email, to_email, client_tran_id, start_time, end_time, page, limit, recv_window) {
  if (!is.null(from_email)) {
    assert_scalar_character(from_email)
  }
  if (!is.null(to_email)) {
    assert_scalar_character(to_email)
  }
  if (!is.null(client_tran_id)) {
    assert_scalar_character(client_tran_id)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(page)) {
    assert_scalar_count(page)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceSubAccount__get_transfer_history <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("tran_id", "from_email", "to_email", "asset", "amount", "create_time_stamp", "from_account_type", "to_account_type", "status", "client_tran_id"))
  assert_double(value[["tran_id"]])
  assert_no_missing_values(value[["tran_id"]])
  assert_character(value[["from_email"]])
  assert_no_missing_values(value[["from_email"]])
  assert_character(value[["to_email"]])
  assert_no_missing_values(value[["to_email"]])
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_character(value[["amount"]])
  assert_no_missing_values(value[["amount"]])
  assert_datetime(value[["create_time_stamp"]])
  assert_no_missing_values(value[["create_time_stamp"]])
  assert_character(value[["from_account_type"]])
  assert_no_missing_values(value[["from_account_type"]])
  assert_character(value[["to_account_type"]])
  assert_no_missing_values(value[["to_account_type"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["client_tran_id"]])
  assert_no_missing_values(value[["client_tran_id"]])
  return(value)
}

assert_args_BinanceSubAccount__get_futures_account <- function(email, futures_type, recv_window) {
  assert_scalar_character(email)
  assert_scalar_count(futures_type)
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceSubAccount__get_futures_account <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_BinanceSubAccount__get_margin_account <- function(email, recv_window) {
  assert_scalar_character(email)
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceSubAccount__get_margin_account <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_BinanceSubAccount__get_status <- function(email, recv_window) {
  if (!is.null(email)) {
    assert_scalar_character(email)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceSubAccount__get_status <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("email", "is_sub_user_enabled", "is_user_active", "insert_time", "is_margin_enabled", "is_future_enabled", "mobile"))
  assert_character(value[["email"]])
  assert_no_missing_values(value[["email"]])
  assert_logical(value[["is_sub_user_enabled"]])
  assert_no_missing_values(value[["is_sub_user_enabled"]])
  assert_logical(value[["is_user_active"]])
  assert_no_missing_values(value[["is_user_active"]])
  assert_datetime(value[["insert_time"]])
  assert_no_missing_values(value[["insert_time"]])
  assert_logical(value[["is_margin_enabled"]])
  assert_no_missing_values(value[["is_margin_enabled"]])
  assert_logical(value[["is_future_enabled"]])
  assert_no_missing_values(value[["is_future_enabled"]])
  assert_integer(value[["mobile"]])
  assert_no_missing_values(value[["mobile"]])
  return(value)
}

assert_args_BinanceTrading__add_order <- function(type, symbol, side, quantity, quote_order_qty, price, time_in_force, new_client_order_id, stop_price, iceberg_qty, new_order_resp_type, self_trade_prevention_mode, recv_window) {
  assert_scalar_character(type)
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  if (!is.null(quantity)) {
    assert_scalar_double(quantity)
  }
  if (!is.null(quote_order_qty)) {
    assert_scalar_double(quote_order_qty)
  }
  if (!is.null(price)) {
    assert_scalar_double(price)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(new_client_order_id)) {
    assert_scalar_character(new_client_order_id)
  }
  if (!is.null(stop_price)) {
    assert_scalar_double(stop_price)
  }
  if (!is.null(iceberg_qty)) {
    assert_scalar_double(iceberg_qty)
  }
  if (!is.null(new_order_resp_type)) {
    assert_scalar_character(new_order_resp_type)
  }
  if (!is.null(self_trade_prevention_mode)) {
    assert_scalar_character(self_trade_prevention_mode)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceTrading__add_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "order_list_id", "client_order_id", "price", "orig_qty", "executed_qty", "cummulative_quote_qty", "status", "time_in_force", "type", "side", "working_time", "self_trade_prevention_mode", "transact_time", "fill_index", "fill_price", "fill_qty", "fill_commission", "fill_commission_asset", "fill_trade_id"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_double(value[["order_list_id"]])
  assert_no_missing_values(value[["order_list_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["cummulative_quote_qty"]])
  assert_no_missing_values(value[["cummulative_quote_qty"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["time_in_force"]])
  assert_no_missing_values(value[["time_in_force"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_datetime(value[["working_time"]])
  assert_no_missing_values(value[["working_time"]])
  assert_character(value[["self_trade_prevention_mode"]])
  assert_no_missing_values(value[["self_trade_prevention_mode"]])
  assert_datetime(value[["transact_time"]])
  assert_no_missing_values(value[["transact_time"]])
  assert_integer(value[["fill_index"]])
  assert_character(value[["fill_price"]])
  assert_character(value[["fill_qty"]])
  assert_character(value[["fill_commission"]])
  assert_character(value[["fill_commission_asset"]])
  assert_double(value[["fill_trade_id"]])
  return(value)
}

assert_args_BinanceTrading__add_order_test <- function(type, symbol, side, quantity, quote_order_qty, price, time_in_force, new_client_order_id, stop_price, iceberg_qty, new_order_resp_type, self_trade_prevention_mode, recv_window) {
  assert_scalar_character(type)
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  if (!is.null(quantity)) {
    assert_scalar_double(quantity)
  }
  if (!is.null(quote_order_qty)) {
    assert_scalar_double(quote_order_qty)
  }
  if (!is.null(price)) {
    assert_scalar_double(price)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(new_client_order_id)) {
    assert_scalar_character(new_client_order_id)
  }
  if (!is.null(stop_price)) {
    assert_scalar_double(stop_price)
  }
  if (!is.null(iceberg_qty)) {
    assert_scalar_double(iceberg_qty)
  }
  if (!is.null(new_order_resp_type)) {
    assert_scalar_character(new_order_resp_type)
  }
  if (!is.null(self_trade_prevention_mode)) {
    assert_scalar_character(self_trade_prevention_mode)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceTrading__add_order_test <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("validated"))
  assert_logical(value[["validated"]])
  assert_no_missing_values(value[["validated"]])
  return(value)
}

assert_args_BinanceTrading__cancel_order <- function(symbol, order_id, orig_client_order_id, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(order_id)) {
    assert_scalar_count(order_id)
  }
  if (!is.null(orig_client_order_id)) {
    assert_scalar_character(orig_client_order_id)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceTrading__cancel_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "orig_client_order_id", "order_id", "order_list_id", "client_order_id", "price", "orig_qty", "executed_qty", "cummulative_quote_qty", "status", "time_in_force", "type", "side", "self_trade_prevention_mode", "transact_time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["orig_client_order_id"]])
  assert_no_missing_values(value[["orig_client_order_id"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_double(value[["order_list_id"]])
  assert_no_missing_values(value[["order_list_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["cummulative_quote_qty"]])
  assert_no_missing_values(value[["cummulative_quote_qty"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["time_in_force"]])
  assert_no_missing_values(value[["time_in_force"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_character(value[["self_trade_prevention_mode"]])
  assert_no_missing_values(value[["self_trade_prevention_mode"]])
  assert_datetime(value[["transact_time"]])
  assert_no_missing_values(value[["transact_time"]])
  return(value)
}

assert_args_BinanceTrading__cancel_all_orders <- function(symbol, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceTrading__cancel_all_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "orig_client_order_id", "order_id", "order_list_id", "client_order_id", "price", "orig_qty", "executed_qty", "cummulative_quote_qty", "status", "time_in_force", "type", "side", "self_trade_prevention_mode", "transact_time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["orig_client_order_id"]])
  assert_no_missing_values(value[["orig_client_order_id"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_double(value[["order_list_id"]])
  assert_no_missing_values(value[["order_list_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["cummulative_quote_qty"]])
  assert_no_missing_values(value[["cummulative_quote_qty"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["time_in_force"]])
  assert_no_missing_values(value[["time_in_force"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_character(value[["self_trade_prevention_mode"]])
  assert_no_missing_values(value[["self_trade_prevention_mode"]])
  assert_datetime(value[["transact_time"]])
  assert_no_missing_values(value[["transact_time"]])
  return(value)
}

assert_args_BinanceTrading__get_order <- function(symbol, order_id, orig_client_order_id, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(order_id)) {
    assert_scalar_count(order_id)
  }
  if (!is.null(orig_client_order_id)) {
    assert_scalar_character(orig_client_order_id)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceTrading__get_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "order_list_id", "client_order_id", "price", "orig_qty", "executed_qty", "cummulative_quote_qty", "status", "time_in_force", "type", "side", "stop_price", "iceberg_qty", "time", "update_time", "is_working", "orig_quote_order_qty", "working_time", "self_trade_prevention_mode"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_double(value[["order_list_id"]])
  assert_no_missing_values(value[["order_list_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["cummulative_quote_qty"]])
  assert_no_missing_values(value[["cummulative_quote_qty"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["time_in_force"]])
  assert_no_missing_values(value[["time_in_force"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_character(value[["stop_price"]])
  assert_no_missing_values(value[["stop_price"]])
  assert_character(value[["iceberg_qty"]])
  assert_no_missing_values(value[["iceberg_qty"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_datetime(value[["update_time"]])
  assert_no_missing_values(value[["update_time"]])
  assert_logical(value[["is_working"]])
  assert_no_missing_values(value[["is_working"]])
  assert_character(value[["orig_quote_order_qty"]])
  assert_no_missing_values(value[["orig_quote_order_qty"]])
  assert_datetime(value[["working_time"]])
  assert_no_missing_values(value[["working_time"]])
  assert_character(value[["self_trade_prevention_mode"]])
  assert_no_missing_values(value[["self_trade_prevention_mode"]])
  return(value)
}

assert_args_BinanceTrading__get_open_orders <- function(symbol, recv_window) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceTrading__get_open_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "order_list_id", "client_order_id", "price", "orig_qty", "executed_qty", "cummulative_quote_qty", "status", "time_in_force", "type", "side", "stop_price", "iceberg_qty", "time", "is_working", "orig_quote_order_qty", "working_time", "self_trade_prevention_mode"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_double(value[["order_list_id"]])
  assert_no_missing_values(value[["order_list_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["cummulative_quote_qty"]])
  assert_no_missing_values(value[["cummulative_quote_qty"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["time_in_force"]])
  assert_no_missing_values(value[["time_in_force"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_character(value[["stop_price"]])
  assert_no_missing_values(value[["stop_price"]])
  assert_character(value[["iceberg_qty"]])
  assert_no_missing_values(value[["iceberg_qty"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_logical(value[["is_working"]])
  assert_no_missing_values(value[["is_working"]])
  assert_character(value[["orig_quote_order_qty"]])
  assert_no_missing_values(value[["orig_quote_order_qty"]])
  assert_datetime(value[["working_time"]])
  assert_no_missing_values(value[["working_time"]])
  assert_character(value[["self_trade_prevention_mode"]])
  assert_no_missing_values(value[["self_trade_prevention_mode"]])
  return(value)
}

assert_args_BinanceTrading__get_all_orders <- function(symbol, order_id, start_time, end_time, limit, recv_window) {
  assert_scalar_character(symbol)
  if (!is.null(order_id)) {
    assert_scalar_count(order_id)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceTrading__get_all_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "order_id", "order_list_id", "client_order_id", "price", "orig_qty", "executed_qty", "cummulative_quote_qty", "status", "time_in_force", "type", "side", "stop_price", "iceberg_qty", "time", "is_working", "orig_quote_order_qty", "working_time", "self_trade_prevention_mode"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_double(value[["order_list_id"]])
  assert_no_missing_values(value[["order_list_id"]])
  assert_character(value[["client_order_id"]])
  assert_no_missing_values(value[["client_order_id"]])
  assert_character(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_character(value[["orig_qty"]])
  assert_no_missing_values(value[["orig_qty"]])
  assert_character(value[["executed_qty"]])
  assert_no_missing_values(value[["executed_qty"]])
  assert_character(value[["cummulative_quote_qty"]])
  assert_no_missing_values(value[["cummulative_quote_qty"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["time_in_force"]])
  assert_no_missing_values(value[["time_in_force"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_character(value[["stop_price"]])
  assert_no_missing_values(value[["stop_price"]])
  assert_character(value[["iceberg_qty"]])
  assert_no_missing_values(value[["iceberg_qty"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_logical(value[["is_working"]])
  assert_no_missing_values(value[["is_working"]])
  assert_character(value[["orig_quote_order_qty"]])
  assert_no_missing_values(value[["orig_quote_order_qty"]])
  assert_datetime(value[["working_time"]])
  assert_no_missing_values(value[["working_time"]])
  assert_character(value[["self_trade_prevention_mode"]])
  assert_no_missing_values(value[["self_trade_prevention_mode"]])
  return(value)
}

assert_args_BinanceTransfer__add_transfer <- function(type, asset, amount, from_symbol, to_symbol, recv_window) {
  assert_scalar_character(type)
  assert_scalar_character(asset)
  assert_scalar_double(amount)
  if (!is.null(from_symbol)) {
    assert_scalar_character(from_symbol)
  }
  if (!is.null(to_symbol)) {
    assert_scalar_character(to_symbol)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceTransfer__add_transfer <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("tran_id"))
  assert_double(value[["tran_id"]])
  assert_no_missing_values(value[["tran_id"]])
  return(value)
}

assert_args_BinanceTransfer__get_transfer_history <- function(type, start_time, end_time, current, size, from_symbol, to_symbol, recv_window) {
  assert_scalar_character(type)
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(current)) {
    assert_scalar_count(current)
  }
  if (!is.null(size)) {
    assert_scalar_count(size)
  }
  if (!is.null(from_symbol)) {
    assert_scalar_character(from_symbol)
  }
  if (!is.null(to_symbol)) {
    assert_scalar_character(to_symbol)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceTransfer__get_transfer_history <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("asset", "amount", "type", "status", "tran_id", "timestamp"))
  assert_character(value[["asset"]])
  assert_no_missing_values(value[["asset"]])
  assert_character(value[["amount"]])
  assert_no_missing_values(value[["amount"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_double(value[["tran_id"]])
  assert_no_missing_values(value[["tran_id"]])
  assert_datetime(value[["timestamp"]])
  assert_no_missing_values(value[["timestamp"]])
  return(value)
}

assert_args_BinanceWithdrawal__add_withdrawal <- function(coin, address, amount, network, withdraw_order_id, address_tag, transaction_fee_flag, name, wallet_type, recv_window) {
  assert_scalar_character(coin)
  assert_scalar_character(address)
  assert_any_of(
    amount,
    function(.x) {
      assert_scalar_double(.x)
    },
    function(.x) {
      assert_scalar_character(.x)
    }
  )
  if (!is.null(network)) {
    assert_scalar_character(network)
  }
  if (!is.null(withdraw_order_id)) {
    assert_scalar_character(withdraw_order_id)
  }
  if (!is.null(address_tag)) {
    assert_scalar_character(address_tag)
  }
  if (!is.null(transaction_fee_flag)) {
    assert_scalar_logical(transaction_fee_flag)
  }
  if (!is.null(name)) {
    assert_scalar_character(name)
  }
  if (!is.null(wallet_type)) {
    assert_scalar_count(wallet_type)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceWithdrawal__add_withdrawal <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id"))
  assert_character(value[["id"]])
  assert_no_missing_values(value[["id"]])
  return(value)
}

assert_args_BinanceWithdrawal__get_withdrawal_history <- function(coin, withdraw_order_id, status, start_time, end_time, offset, limit, recv_window) {
  if (!is.null(coin)) {
    assert_scalar_character(coin)
  }
  if (!is.null(withdraw_order_id)) {
    assert_scalar_character(withdraw_order_id)
  }
  if (!is.null(status)) {
    assert_scalar_count(status)
  }
  if (!is.null(start_time)) {
    assert_scalar_count(start_time)
  }
  if (!is.null(end_time)) {
    assert_scalar_count(end_time)
  }
  if (!is.null(offset)) {
    assert_scalar_count(offset)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
  }
  return(invisible(NULL))
}

assert_return_BinanceWithdrawal__get_withdrawal_history <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id", "amount", "transaction_fee", "coin", "status", "address", "tx_id", "apply_time", "network", "transfer_type", "withdraw_order_id", "info", "confirm_no", "wallet_type", "tx_key", "complete_time"))
  assert_character(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_character(value[["amount"]])
  assert_no_missing_values(value[["amount"]])
  assert_character(value[["transaction_fee"]])
  assert_no_missing_values(value[["transaction_fee"]])
  assert_character(value[["coin"]])
  assert_no_missing_values(value[["coin"]])
  assert_integer(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["address"]])
  assert_no_missing_values(value[["address"]])
  assert_character(value[["tx_id"]])
  assert_no_missing_values(value[["tx_id"]])
  assert_datetime(value[["apply_time"]])
  assert_no_missing_values(value[["apply_time"]])
  assert_character(value[["network"]])
  assert_no_missing_values(value[["network"]])
  assert_integer(value[["transfer_type"]])
  assert_no_missing_values(value[["transfer_type"]])
  assert_character(value[["withdraw_order_id"]])
  assert_no_missing_values(value[["withdraw_order_id"]])
  assert_character(value[["info"]])
  assert_no_missing_values(value[["info"]])
  assert_integer(value[["confirm_no"]])
  assert_no_missing_values(value[["confirm_no"]])
  assert_integer(value[["wallet_type"]])
  assert_no_missing_values(value[["wallet_type"]])
  assert_character(value[["tx_key"]])
  assert_no_missing_values(value[["tx_key"]])
  assert_datetime(value[["complete_time"]])
  return(value)
}

assert_args_BinanceWsBase__initialize <- function(base_url, auto_reconnect, max_reconnects, proactive_reconnect) {
  assert_scalar_character(base_url)
  assert_scalar_logical(auto_reconnect)
  assert_scalar_count(max_reconnects)
  assert_between(max_reconnects, lower = 1)
  assert_scalar_logical(proactive_reconnect)
  return(invisible(NULL))
}

assert_return_BinanceWsBase__initialize <- function(value) {
  assert_class(value, "BinanceWsBase")
  return(value)
}

assert_args_BinanceWsBase__on <- function(event, handler) {
  assert_scalar_character(event)
  assert_value_in_set(event, c("open", "message", "close", "error"))
  assert_function(handler)
  return(invisible(NULL))
}

assert_return_BinanceWsBase__on <- function(value) {
  assert_class(value, "BinanceWsBase")
  return(value)
}

assert_args_BinanceWsBase__subscribe <- function(streams) {
  assert_character(streams)
  assert_no_missing_values(streams)
  return(invisible(NULL))
}

assert_return_BinanceWsBase__subscribe <- function(value) {
  assert_class(value, "BinanceWsBase")
  return(value)
}

assert_args_BinanceWsBase__unsubscribe <- function(streams) {
  assert_character(streams)
  assert_no_missing_values(streams)
  return(invisible(NULL))
}

assert_return_BinanceWsBase__unsubscribe <- function(value) {
  assert_class(value, "BinanceWsBase")
  return(value)
}

assert_return_BinanceWsBase__subscriptions <- function(value) {
  assert_character(value)
  assert_no_missing_values(value)
  return(value)
}

assert_args_binance_backfill_klines <- function(timeframes, from, to, file, base_url, sleep, verbose, timeout, max_tries) {
  assert_character(timeframes)
  assert_no_missing_values(timeframes)
  assert_any_of(
    from,
    function(.x) {
      assert_scalar_datetime(.x)
    },
    function(.x) {
      assert_scalar_double(.x)
    }
  )
  assert_any_of(
    to,
    function(.x) {
      assert_scalar_datetime(.x)
    },
    function(.x) {
      assert_scalar_double(.x)
    }
  )
  assert_scalar_character(file)
  assert_scalar_character(base_url)
  assert_scalar_double(sleep)
  assert_between(sleep, lower = 0, upper = Inf, upper_inclusive = FALSE)
  assert_scalar_logical(verbose)
  assert_any_of(
    timeout,
    function(.x) {
      assert_scalar_double(.x)
      assert_between(.x, lower = 0, lower_inclusive = FALSE, upper = Inf, upper_inclusive = FALSE)
    },
    function(.x) {
      assert_scalar_integer(.x)
      assert_between(.x, lower = 1)
    }
  )
  assert_scalar_count(max_tries)
  assert_between(max_tries, lower = 1)
  return(invisible(NULL))
}

assert_return_binance_backfill_klines <- function(value) {
  assert_scalar_character(value)
  return(value)
}

assert_args_utc_string_to_datetime <- function(x) {
  if (!is.null(x)) {
    assert_character(x)
    assert_no_missing_values(x)
  }
  return(invisible(NULL))
}

assert_return_utc_string_to_datetime <- function(value) {
  assert_datetime(value)
  return(value)
}

assert_args_parse_orderbook <- function(data) {
  if (!is.null(data)) {
    assert_list(data)
  }
  return(invisible(NULL))
}

assert_return_parse_orderbook <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("last_update_id", "side", "price", "size"))
  assert_character(value[["last_update_id"]])
  assert_no_missing_values(value[["last_update_id"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_double(value[["price"]])
  assert_no_missing_values(value[["price"]])
  assert_double(value[["size"]])
  assert_no_missing_values(value[["size"]])
  return(value)
}

assert_args_parse_paginated <- function(data, time_cols) {
  if (!is.null(data)) {
    assert_list(data)
  }
  assert_character(time_cols)
  assert_no_missing_values(time_cols)
  return(invisible(NULL))
}

assert_return_parse_paginated <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_parse_klines <- function(data) {
  if (!is.null(data)) {
    assert_list(data)
  }
  return(invisible(NULL))
}

assert_return_parse_klines <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("datetime", "open", "high", "low", "close", "volume", "close_time", "quote_volume", "trades", "taker_buy_base_volume", "taker_buy_quote_volume", "ignore"))
  assert_datetime(value[["datetime"]])
  assert_no_missing_values(value[["datetime"]])
  assert_double(value[["open"]])
  assert_no_missing_values(value[["open"]])
  assert_double(value[["high"]])
  assert_no_missing_values(value[["high"]])
  assert_double(value[["low"]])
  assert_no_missing_values(value[["low"]])
  assert_double(value[["close"]])
  assert_no_missing_values(value[["close"]])
  assert_double(value[["volume"]])
  assert_no_missing_values(value[["volume"]])
  assert_datetime(value[["close_time"]])
  assert_no_missing_values(value[["close_time"]])
  assert_double(value[["quote_volume"]])
  assert_no_missing_values(value[["quote_volume"]])
  assert_integer(value[["trades"]])
  assert_no_missing_values(value[["trades"]])
  assert_double(value[["taker_buy_base_volume"]])
  assert_no_missing_values(value[["taker_buy_base_volume"]])
  assert_double(value[["taker_buy_quote_volume"]])
  assert_no_missing_values(value[["taker_buy_quote_volume"]])
  assert_character(value[["ignore"]])
  assert_no_missing_values(value[["ignore"]])
  return(value)
}

assert_args_validate_order_params <- function(type, symbol, side, quantity, quote_order_qty, price, time_in_force, new_client_order_id, stop_price, iceberg_qty, new_order_resp_type, self_trade_prevention_mode, recv_window) {
  assert_scalar_character(type)
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  if (!is.null(quantity)) {
    assert_scalar_double(quantity)
  }
  if (!is.null(quote_order_qty)) {
    assert_scalar_double(quote_order_qty)
  }
  if (!is.null(price)) {
    assert_scalar_double(price)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(new_client_order_id)) {
    assert_scalar_character(new_client_order_id)
  }
  if (!is.null(stop_price)) {
    assert_scalar_double(stop_price)
  }
  if (!is.null(iceberg_qty)) {
    assert_scalar_double(iceberg_qty)
  }
  if (!is.null(new_order_resp_type)) {
    assert_scalar_character(new_order_resp_type)
  }
  if (!is.null(self_trade_prevention_mode)) {
    assert_scalar_character(self_trade_prevention_mode)
  }
  if (!is.null(recv_window)) {
    assert_scalar_count(recv_window)
    assert_between(recv_window, lower = 1)
  }
  return(invisible(NULL))
}

assert_return_validate_order_params <- function(value) {
  assert_list(value)
  return(value)
}

assert_args_ws_control_message <- function(method, params, id) {
  assert_scalar_character(method)
  assert_value_in_set(method, c("SUBSCRIBE", "UNSUBSCRIBE"))
  assert_character(params)
  assert_no_missing_values(params)
  assert_scalar_count(id)
  assert_between(id, lower = 1)
  return(invisible(NULL))
}

assert_return_ws_control_message <- function(value) {
  assert_scalar_character(value)
  return(value)
}

assert_args_ws_depth_stream <- function(symbol, speed) {
  assert_scalar_character(symbol)
  assert_scalar_character(speed)
  assert_value_in_set(speed, c("1000ms", "100ms"))
  return(invisible(NULL))
}

assert_return_ws_depth_stream <- function(value) {
  assert_scalar_character(value)
  return(value)
}

assert_args_ws_backoff_delay <- function(attempt, cap_seconds) {
  assert_scalar_count(attempt)
  assert_between(attempt, lower = 1)
  assert_scalar_double(cap_seconds)
  assert_between(cap_seconds, lower = 0, lower_inclusive = FALSE, upper = Inf, upper_inclusive = FALSE)
  return(invisible(NULL))
}

assert_return_ws_backoff_delay <- function(value) {
  assert_scalar_double(value)
  assert_between(value, lower = 1, upper = Inf, upper_inclusive = FALSE)
  return(value)
}

assert_args_get_base_url <- function(url) {
  if (!is.null(url)) {
    assert_scalar_character(url)
  }
  return(invisible(NULL))
}

assert_return_get_base_url <- function(value) {
  assert_scalar_character(value)
  return(value)
}

assert_args_get_api_keys <- function(api_key, api_secret) {
  assert_scalar_character(api_key)
  assert_scalar_character(api_secret)
  return(invisible(NULL))
}

assert_return_get_api_keys <- function(value) {
  assert_list(value)
  assert_has_names(value, c("api_key", "api_secret"))
  assert_scalar_character(value[["api_key"]])
  assert_scalar_character(value[["api_secret"]])
  return(value)
}

assert_args_verify_symbol <- function(ticker) {
  assert_scalar_character(ticker)
  return(invisible(NULL))
}

assert_return_verify_symbol <- function(value) {
  assert_scalar_logical(value)
  return(value)
}

assert_args_get_futures_base_url <- function(url) {
  if (!is.null(url)) {
    assert_scalar_character(url)
  }
  return(invisible(NULL))
}

assert_return_get_futures_base_url <- function(value) {
  assert_scalar_character(value)
  return(value)
}

assert_args_time_convert_from_binance <- function(unit) {
  assert_scalar_character(unit)
  assert_value_in_set(unit, c("ms", "ns", "s"))
  return(invisible(NULL))
}

assert_return_time_convert_from_binance <- function(value) {
  assert_scalar_datetime(value)
  return(value)
}

assert_args_time_convert_to_binance <- function(unit) {
  assert_scalar_character(unit)
  assert_value_in_set(unit, c("ms", "ns", "s"))
  return(invisible(NULL))
}

assert_return_time_convert_to_binance <- function(value) {
  assert_any_of(
    value,
    function(.x) {
      assert_scalar_double(.x)
    },
    function(.x) {
      assert_scalar_integer(.x)
    }
  )
  return(value)
}

