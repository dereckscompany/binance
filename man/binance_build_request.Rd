% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_request.R
\name{binance_build_request}
\alias{binance_build_request}
\title{Build and Execute a Binance API Request}
\usage{
binance_build_request(
  base_url,
  endpoint,
  method = "GET",
  query = list(),
  body = NULL,
  keys = NULL,
  .perform = httr2::req_perform,
  .parser = identity,
  is_async = FALSE,
  timeout = 10,
  .get_timestamp_ms = NULL
)
}
\arguments{
\item{base_url}{Character; the API base URL.}

\item{endpoint}{Character; the API path.}

\item{method}{Character; HTTP method. Default \code{"GET"}.}

\item{query}{Named list; query parameters. Default \code{list()}.}

\item{body}{Named list or NULL; request body (for POST). Default \code{NULL}.}

\item{keys}{List or NULL; API credentials. Default \code{NULL}.}

\item{.perform}{Function; the httr2 perform function. Default \code{httr2::req_perform}.}

\item{.parser}{Function; post-processing function applied to parsed response.
Default \code{identity}.}

\item{is_async}{Logical; whether \code{.perform} returns promises. Default \code{FALSE}.}

\item{timeout}{Numeric; request timeout in seconds. Default \code{10}.}

\item{.get_timestamp_ms}{Function or NULL; zero-argument function returning
epoch milliseconds for HMAC signing.}
}
\value{
Parsed and post-processed API response data, or a promise thereof.
}
\description{
Constructs an \link[httr2:request]{httr2::request}, optionally signs it, performs it via the supplied
\code{.perform} function, and parses the JSON response. This is the single
point through which all Binance API calls flow.
}
