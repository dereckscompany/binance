% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backfill.R
\name{binance_backfill_klines}
\alias{binance_backfill_klines}
\title{Backfill Binance Kline Data to CSV}
\usage{
binance_backfill_klines(
  symbols,
  timeframes = "1d",
  from = lubridate::now("UTC") - lubridate::ddays(365),
  to = lubridate::now("UTC"),
  file = "binance_klines.csv",
  base_url = "https://api.binance.com",
  sleep = 0.3,
  verbose = TRUE
)
}
\arguments{
\item{symbols}{(character) trading pair symbols (e.g.,
\code{c("BTCUSDT", "ETHUSDT")}). Must not be NULL or empty.}

\item{timeframes}{(character) candle timeframes (e.g., \code{c("1d", "1h")}).
Valid values: \code{"1s"}, \code{"1m"}, \code{"3m"}, \code{"5m"}, \code{"15m"}, \code{"30m"}, \code{"1h"},
\code{"2h"}, \code{"4h"}, \code{"6h"}, \code{"8h"}, \code{"12h"}, \code{"1d"}, \code{"3d"}, \code{"1w"}, \code{"1M"}.}

\item{from}{(scalar<POSIXct> | scalar<numeric>) start of the backfill window.
Defaults to one year ago. Values before \code{"2017-07-01"} (or \code{-Inf}) are
clamped to \code{"2017-07-01"} since Binance data does not exist before that date.}

\item{to}{(scalar<POSIXct> | scalar<numeric>) end of the backfill window.
Defaults to current time. \code{Inf} is replaced with current time.}

\item{file}{(scalar<character>) path to the output CSV file. Data is appended
incrementally so progress is saved even if the process is interrupted.}

\item{base_url}{(scalar<character>) Binance API base URL.}

\item{sleep}{(scalar<numeric in [0, Inf[>) seconds to sleep between each
symbol-timeframe combination to respect rate limits.}

\item{verbose}{(scalar<logical>) if \code{TRUE}, prints progress messages via \code{\link[rlang:abort]{rlang::inform()}}.}
}
\value{
(scalar<character>) the file path (invisibly).

Per-combo failures are surfaced as warnings during the run (one
\code{rlang::warn()} per failed \verb{(symbol, timeframe)} pair, with the
underlying error message). After the loop, if any combinations
failed, a final summary warning lists the count and the affected
pairs. No failure data is hidden on the return value.
}
\description{
Downloads historical OHLCV candlestick data for one or more trading pairs and
timeframes, writing results incrementally to a CSV file. Supports resuming
from a partially completed backfill by reading the existing file and skipping
symbol-timeframe combinations that are already up to date.
}
\details{
Only \strong{closed} candles are persisted: the candle still forming at the live
edge (one whose \code{close_time} is in the future) is dropped before writing, so
a half-built candle is never stored. Because resume advances past the last
stored candle, an unclosed candle written once would never be refreshed to
its final values — dropping it means the next run re-fetches and completes
it. Closed historical candles are unaffected.
}
\examples{
\dontrun{
binance_backfill_klines(
  symbols = c("BTCUSDT", "ETHUSDT"),
  timeframes = c("1d", "1h"),
  from = lubridate::as_datetime("2020-01-01"),
  file = "my_klines.csv"
)
}
}
