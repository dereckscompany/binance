% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/types_binance.R
\name{binance_shapes}
\alias{binance_shapes}
\title{Binance REST return shapes}
\description{
Reusable roxyassert \verb{@type} shapes for the \code{data.table}s returned
by the Binance REST endpoint classes. A method documents its return as
\verb{(promise<Shape>)} (a Binance call returns a value in sync mode or a
\code{promises::promise} resolving to the same value in async mode), and the
contract roclet expands the shape into the generated \verb{assert_return_*} helper.
The helper is wired at the call site through
\code{\link[connectcore:then_or_now]{connectcore::then_or_now()}} so the row-and-column contract runs in BOTH
execution modes.

Each column is typed to what the parser actually produces (verified against
the parse helpers in \code{R/helpers_parse.R} and the \code{tests/testthat} fixtures).
Binance returns most numeric quantities as JSON \strong{strings}, so price/size/
quantity columns are \code{character} unless a parser explicitly coerces them
(klines and the order book coerce to \code{numeric}; \code{numeric} is the strict
double, per the package convention). Millisecond timestamps the parsers run
through \code{ms_to_datetime()} become \code{POSIXct}. A column is marked \verb{| NA} only
where the value can legitimately be missing in the parsed result.

Only shapes returned by three or more methods, or that derive from one another
(\code{extends} / \code{pick} / \code{omit}), are declared here; a one-off return is written
inline at its method with the same column-bullet grammar.

\verb{@genassert} emits a standalone \verb{assert_type_<Shape>()} for every shape, and
\verb{@exportassert} exports them so a downstream package can validate against
Binance's shapes.
}
